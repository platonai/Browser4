/**
 * Browser4 CLI — command-line interface for the Browser4 automation platform.
 *
 * Exported types and helpers used when the CLI is embedded as a library.
 */

export { Browser4Client, Browser4ClientConfig } from './client';
export { VERSION } from './version';
