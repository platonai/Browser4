# The embedded MCP-over-HTTP server (Ktor, default port 8088) is an optional
# add-on that collides across the multiple Spring test contexts sharing this
# Surefire JVM (forkCount=1, reuseForks=true). REST tests exercise the Spring
# MVC MCPToolController instead, so disable the Ktor server here.
mcp.http.enabled=false
