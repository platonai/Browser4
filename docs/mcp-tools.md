# Browser4 MCP tool reference

<!-- GENERATED by ToolDocGenerator — do not edit by hand. -->
<!-- Regenerate: mvn -pl browser4-agentic test -Dtest=ToolDocGeneratorTest -DregenerateToolDocs=true -->

137 tools across 9 domains.

## Domain `browser`

### `browser_close_tab`

```
browser.closeTab(index: Int, tabId: String)
```

Close a tab by zero-based index or GUID, or the current tab when omitted

| Argument | Type | Required | Default | Meaning |
|---|---|---|---|---|
| `index` | Int | yes |  | index: Int |
| `tabId` | String | yes |  | tabId: String |

Returns: `Boolean`

### `browser_list_tabs`

```
browser.listTabs()
```

List all tabs with index, guid, title, and url

Returns: `List<Map<String, String>>`

### `browser_new_tab`

```
browser.newTab(url: String = about:blank)
```

Create a new tab. Returns guid and url

| Argument | Type | Required | Default | Meaning |
|---|---|---|---|---|
| `url` | String | no | about:blank | url: String = about:blank |

Returns: `Map<String, String>`

### `browser_switch_tab`

```
browser.switchTab(index: Int, tabId: String)
```

Switch to a specific browser tab by its zero-based index or GUID

| Argument | Type | Required | Default | Meaning |
|---|---|---|---|---|
| `index` | Int | yes |  | index: Int |
| `tabId` | String | yes |  | tabId: String |

Returns: `WebDriver`

## Domain `command`

### `command_result`

```
command.result(id: String)
```

Get the result of a completed command task by its task ID.

| Argument | Type | Required | Default | Meaning |
|---|---|---|---|---|
| `id` | String | yes |  | Task id returned by `command.run`. Required. |

Returns: `CommandResult`

Examples:

- Read the finished command's output: `{"id": "5c3a1f2e-9b47-4d21-8f0a-1e6b7c8d9a01"}`

### `command_run`

```
command.run(command: String, async: Boolean = true, noopLimit: Int, engine: String)
```

Execute a plain command (URL, instruction, or agent task). When async=true (default), returns a task ID immediately. When async=false, blocks until done and returns the CommandStatus as JSON. noopLimit optionally overrides the consecutive no-op abort threshold for agent tasks. engine optionally selects the agent execution engine: 'cli' (default) for the CLI tool-loop engine; 'observe-act' is the DEPRECATED legacy engine.

| Argument | Type | Required | Default | Meaning |
|---|---|---|---|---|
| `command` | String | yes |  | What to run: a URL to load, a natural-language instruction, or an agent task. Required. |
| `async` | Boolean | no | true | `true` (default) returns a task id immediately; `false` blocks until the command finishes and returns the CommandStatus JSON. |
| `noopLimit` | Int | yes |  | Consecutive no-op abort threshold for agent tasks; omit to use the server default. |
| `engine` | String | yes |  | Agent engine: `cli` (default) for the tool-loop engine, `observe-act` is deprecated. |

Returns: `String`

Examples:

- Load a page asynchronously: `{"command": "https://example.com"}`
  - Returns a task id; poll it with command.status
- Run an agent task and wait: `{"command": "collect the page title", "async": "false"}`

### `command_status`

```
command.status(id: String)
```

Get the status of a previously submitted command task by its task ID.

| Argument | Type | Required | Default | Meaning |
|---|---|---|---|---|
| `id` | String | yes |  | Task id returned by `command.run`. Required. |

Returns: `CommandStatus`

Examples:

- Poll a running command: `{"id": "5c3a1f2e-9b47-4d21-8f0a-1e6b7c8d9a01"}`

## Domain `crawl`

### `crawl_result`

```
crawl.result(id: String)
```

Get the result of a completed crawl task by its task ID.

| Argument | Type | Required | Default | Meaning |
|---|---|---|---|---|
| `id` | String | yes |  | Task id returned by `crawl.submit`. |

Returns: `CrawlResponse`

Examples:

- Read the collected pages: `{"id": "8f14e45f-ea0b-4c1e-9d3a-2f6d5c4b1a09"}`

### `crawl_status`

```
crawl.status(id: String)
```

Get the status/result of a crawl task by its task ID.

| Argument | Type | Required | Default | Meaning |
|---|---|---|---|---|
| `id` | String | yes |  | Task id returned by `crawl.submit`. |

Returns: `CrawlResponse`

Examples:

- Poll a submitted task: `{"id": "8f14e45f-ea0b-4c1e-9d3a-2f6d5c4b1a09"}`
  - An unknown id returns a 404-style CrawlResponse rather than an error

### `crawl_submit`

```
crawl.submit(url: String, depth: Int = 1, args: String = "")
```

Submit a crawl task. Returns a task ID for status polling.

| Argument | Type | Required | Default | Meaning |
|---|---|---|---|---|
| `url` | String | yes |  | Seed URL to start from. Required unless the seed list is supplied through `args`. |
| `depth` | Int | no | 1 | How many link levels to follow from the seed. `0` processes the seed page only. |
| `args` | String | no |  | Extra crawl arguments as a CLI-style string, e.g. `-outLinkSelector=a[href]` or an X-SQL query. |

Returns: `String`

Examples:

- Crawl one link level from a seed URL: `{"url": "https://example.com", "depth": "1"}`
- Seed page only, then poll: `{"url": "https://example.com", "depth": "0"}`
  - Feed the returned task id to crawl.status

<details><summary>Full documentation</summary>

Starts a recursive crawl in the background and returns the task id
immediately. Poll it with `crawl.status`, read the payload with
`crawl.result`. Use `depth=0` for a single page, and prefer the
swarm domain for high-throughput parallel extraction.

</details>

## Domain `experience`

### `experience_deep_learn`

```
experience.deep_learn(url: String, intent: String, force: Boolean = false)
```

Deep Learning: run analysis tools (htmlsnapshot summary, inspect) to build or update KnowledgeFacts. Creates hypothesis on first run, promotes to verified when confidence threshold met. Use force=true to bypass sampling checks.

| Argument | Type | Required | Default | Meaning |
|---|---|---|---|---|
| `url` | String | yes |  | url: String |
| `intent` | String | yes |  | intent: String |
| `force` | Boolean | no | false | force: Boolean = false |

Returns: `String`

### `experience_list`

```
experience.list(filter: String, intent_filter: String, page: Int = 1, page_size: Int = 20)
```

List stored knowledge entries organized by domain + intent. Filter by domain (filter) or intent (intent_filter). Paginated.

| Argument | Type | Required | Default | Meaning |
|---|---|---|---|---|
| `filter` | String | yes |  | filter: String |
| `intent_filter` | String | yes |  | intent_filter: String |
| `page` | Int | no | 1 | page: Int = 1 |
| `page_size` | Int | no | 20 | page_size: Int = 20 |

Returns: `String`

### `experience_query`

```
experience.query(url: String, intent: String)
```

Query stored knowledge with intent-based resolution. Classifies intent, then resolves: (domain,intent) → (domain,url) → (family,intent) → (category,intent) → (universal,intent) → cold start. Returns tier, confidence, selectors, blockers, warnings, and status.

| Argument | Type | Required | Default | Meaning |
|---|---|---|---|---|
| `url` | String | yes |  | url: String |
| `intent` | String | yes |  | intent: String |

Returns: `String`

### `experience_save`

```
experience.save(url: String, trace: String, outcome: String = success, intent: String, task_type: String, facts: String)
```

Fast Learning: save task trace and update experience stats. Runs in ~tens of ms. No analysis tools executed. Use experience_deep_learn for analysis and knowledge promotion. Optional `facts` (JSON string or object) merges retrospective knowledge (selectors/interaction_hints/known_blockers/anti_patterns) into the (domain, intent) facts entry — the writer path for lessons learned on a task. Refused when the entry is VERIFIED (immutable).

| Argument | Type | Required | Default | Meaning |
|---|---|---|---|---|
| `url` | String | yes |  | url: String |
| `trace` | String | yes |  | trace: String |
| `outcome` | String | no | success | outcome: String = success |
| `intent` | String | yes |  | intent: String |
| `task_type` | String | yes |  | task_type: String |
| `facts` | String | yes |  | facts: String |

Returns: `String`

## Domain `html_snapshot`

### `html_snapshot_capture`

```
html_snapshot.capture(sessionId: String)
```

Capture the current page as an HTML snapshot with metadata, interactive elements, and link groups.

| Argument | Type | Required | Default | Meaning |
|---|---|---|---|---|
| `sessionId` | String | yes |  | sessionId: String |

Returns: `String`

### `html_snapshot_export`

```
html_snapshot.export(sessionId: String, clean: Boolean = false)
```

Export the full, pretty-printed HTML of the current page. Set clean=true to strip <script>, <style>, and non-standard attributes (keeps the vi attribute).

| Argument | Type | Required | Default | Meaning |
|---|---|---|---|---|
| `sessionId` | String | yes |  | sessionId: String |
| `clean` | Boolean | no | false | clean: Boolean = false |

Returns: `String`

### `html_snapshot_inspect`

```
html_snapshot.inspect(sessionId: String, selector: String = :root, max: Int = 20, depth: Int = 5)
```

Inspect the HTML snapshot and suggest CSS selectors for recurring patterns.

| Argument | Type | Required | Default | Meaning |
|---|---|---|---|---|
| `sessionId` | String | yes |  | sessionId: String |
| `selector` | String | no | :root | selector: String = :root |
| `max` | Int | no | 20 | max: Int = 20 |
| `depth` | Int | no | 5 | depth: Int = 5 |

Returns: `String`

### `html_snapshot_query`

```
html_snapshot.query(sql: String, url: String, sessionId: String)
```

Execute an X-SQL query against the current page or a specified URL.

| Argument | Type | Required | Default | Meaning |
|---|---|---|---|---|
| `sql` | String | yes |  | sql: String |
| `url` | String | yes |  | url: String |
| `sessionId` | String | yes |  | sessionId: String |

Returns: `String`

### `html_snapshot_readability`

```
html_snapshot.readability(sessionId: String, url: String)
```

Extract the main article content (title, byline, site name, excerpt, cleaned HTML, plain text) from the stored HTML snapshot using a Readability-style heuristic. When url is given, the page is fetched independently; otherwise the current session page is used.

| Argument | Type | Required | Default | Meaning |
|---|---|---|---|---|
| `sessionId` | String | yes |  | sessionId: String |
| `url` | String | yes |  | url: String |

Returns: `String`

### `html_snapshot_scrape`

```
html_snapshot.scrape(sessionId: String, field: String, selector: String = :root, attrName: String)
```

Extract text, textcontent, html, or an attribute value from a single element matching a CSS selector.

| Argument | Type | Required | Default | Meaning |
|---|---|---|---|---|
| `sessionId` | String | yes |  | sessionId: String |
| `field` | String | yes |  | field: String |
| `selector` | String | no | :root | selector: String = :root |
| `attrName` | String | yes |  | attrName: String |

Returns: `String`

### `html_snapshot_scrape_all`

```
html_snapshot.scrape_all(sessionId: String, field: String, selector: String = :root, attrName: String, offset: Int = 0, limit: Int = -1)
```

Extract text, textcontent, html, or attribute values from ALL elements matching a CSS selector.

| Argument | Type | Required | Default | Meaning |
|---|---|---|---|---|
| `sessionId` | String | yes |  | sessionId: String |
| `field` | String | yes |  | field: String |
| `selector` | String | no | :root | selector: String = :root |
| `attrName` | String | yes |  | attrName: String |
| `offset` | Int | no | 0 | offset: Int = 0 |
| `limit` | Int | no | -1 | limit: Int = -1 |

Returns: `String`

### `html_snapshot_summary`

```
html_snapshot.summary(sessionId: String)
```

Generate a page summary including title, statistics, and detected link groups.

| Argument | Type | Required | Default | Meaning |
|---|---|---|---|---|
| `sessionId` | String | yes |  | sessionId: String |

Returns: `String`

## Domain `memory`

### `memory_forget`

```
memory.forget(taskId: String)
```

Explicitly forget one task from memory (privacy / correction). Removes its events from the log and the search index.

| Argument | Type | Required | Default | Meaning |
|---|---|---|---|---|
| `taskId` | String | yes |  | taskId: String |

Returns: `String`

### `memory_note`

```
memory.note(key: String, value: String, taskId: String)
```

Write one working-memory note. Notes are re-injected into the conversation every round and survive context compression — use them for stable cross-step conclusions, confirmed assumptions, and pending todos.

| Argument | Type | Required | Default | Meaning |
|---|---|---|---|---|
| `key` | String | yes |  | key: String |
| `value` | String | yes |  | value: String |
| `taskId` | String | yes |  | taskId: String |

Returns: `String`

### `memory_read`

```
memory.read(taskId: String, seq: Long, before: Int = 0, after: Int = 0)
```

Read a bounded window of memory events of one task around the event seq (defaults to the whole task, newest last). Bounded by the configured read window.

| Argument | Type | Required | Default | Meaning |
|---|---|---|---|---|
| `taskId` | String | yes |  | taskId: String |
| `seq` | Long | yes |  | seq: Long |
| `before` | Int | no | 0 | before: Int = 0 |
| `after` | Int | no | 0 | after: Int = 0 |

Returns: `String`

### `memory_search`

```
memory.search(query: String, agent: String, limit: Int = 10)
```

Search agent memory (past tasks and tool executions) by keywords. Returns hits with taskId, timestamp, tool and a snippet; use memory.read to fetch details. Optionally restrict to one agent uuid (agent).

| Argument | Type | Required | Default | Meaning |
|---|---|---|---|---|
| `query` | String | yes |  | query: String |
| `agent` | String | yes |  | agent: String |
| `limit` | Int | no | 10 | limit: Int = 10 |

Returns: `String`

## Domain `skill`

### `skill_info`

```
skill.info(id: String)
```

Get detailed information about a skill by ID.

| Argument | Type | Required | Default | Meaning |
|---|---|---|---|---|
| `id` | String | yes |  | Skill id as reported by `skill.list`. Required. |

Returns: `SkillDetail`

Examples:

- Inspect a bundled skill: `{"id": "browser4-cli"}`

### `skill_install`

```
skill.install(path: String, overwrite: Boolean = false)
```

Install a skill from a server-side directory path containing SKILL.md.

| Argument | Type | Required | Default | Meaning |
|---|---|---|---|---|
| `path` | String | yes |  | Server-side directory containing SKILL.md. Required; the path resolves on the server. |
| `overwrite` | Boolean | no | false | Replace an existing skill with the same id. |

Returns: `InstallResult`

Examples:

- Install a skill from a server directory: `{"path": "/opt/skills/my-skill"}`

### `skill_list`

```
skill.list()
```

List all registered skills with lightweight summaries.

Returns: `List<SkillSummary>`

Examples:


### `skill_reload`

```
skill.reload(id: String)
```

Reload a skill from its source directory.

| Argument | Type | Required | Default | Meaning |
|---|---|---|---|---|
| `id` | String | yes |  | Skill id to reload from its source directory. Required. |

Returns: `Map`

Examples:

- Reload after editing SKILL.md: `{"id": "browser4-cli"}`

### `skill_uninstall`

```
skill.uninstall(id: String)
```

Uninstall a skill by ID.

| Argument | Type | Required | Default | Meaning |
|---|---|---|---|---|
| `id` | String | yes |  | Skill id to remove. Required. |

Returns: `InstallResult`

Examples:

- Remove a skill: `{"id": "my-skill"}`

## Domain `tab`

### `aria_snapshot`, `browser_snapshot`

```
tab.ariaSnapshot(options: AriaSnapshotOptions)
```

Return the ARIA snapshot (accessibility tree in YAML format) with filtering [options] applied.

| Argument | Type | Required | Default | Meaning |
|---|---|---|---|---|
| `options` | AriaSnapshotOptions | yes |  | The filtering and rendering options. |

Returns: `String`

<details><summary>Full documentation</summary>

Return the ARIA snapshot (accessibility tree in YAML format) with filtering [options] applied.

Supports interactive-only mode, URL inclusion, compact mode, depth limiting, CSS selector scoping, and viewport filtering. All options compose.

</details>

### `base_u_r_i`

```
tab.baseURI()
```

Returns the document's baseURI.

Returns: `String`

<details><summary>Full documentation</summary>

Returns the document's baseURI.

The baseURI is a property of Node, it's the absolute base URL of the document containing the node. A baseURI is used to resolve relative URLs.

The base URL is determined as follows: 1. By default, the base URL is the location of the document (as determined by window.location). 2. If the document has an `<base>` element, its href attribute is used.

</details>

### `bounding_box`

```
tab.boundingBox(selector: String)
```

Return the bounding box of an element located by [selector].

| Argument | Type | Required | Default | Meaning |
|---|---|---|---|---|
| `selector` | String | yes |  | The selector of the element, multiple formats supported. Used to calculate the bounding box. |

Returns: `RectD?`

<details><summary>Full documentation</summary>

Return the bounding box of an element located by [selector].

If the element does not exist, returns null.

</details>

### `bring_to_front`

```
tab.bringToFront()
```

Brings the browser window to the front.

Returns: `Unit`

Examples:

- Example usage:

  ```kotlin
  * driver.bringToFront()
       *
  ```

<details><summary>Full documentation</summary>

Brings the browser window to the front.

```kotlin driver.bringToFront() ```

</details>

### `check`, `browser_check`

```
tab.check(selector: String)
```

This method check an element with [selector]. If there's no element matching [selector], nothing to do.

| Argument | Type | Required | Default | Meaning |
|---|---|---|---|---|
| `selector` | String | yes |  | The selector of the element, multiple formats supported. If there are multiple matching elements, the first will be checked. |

Returns: `Unit`

<details><summary>Full documentation</summary>

This method check an element with [selector]. If there's no element matching [selector], nothing to do.

elements, the first will be checked.

</details>

### `clear_browser_cookies`

```
tab.clearBrowserCookies()
```

Clears browser cookies.

Returns: `Unit`

Examples:

- Example usage:

  ```kotlin
  * driver.clearBrowserCookies()
       *
  ```

<details><summary>Full documentation</summary>

Clears browser cookies.

```kotlin driver.clearBrowserCookies() ```

</details>

### `click`

```
tab.click(selector: String, modifier: String)
```

Focus on an element with [selector] and click it with [modifier] pressed.

| Argument | Type | Required | Default | Meaning |
|---|---|---|---|---|
| `selector` | String | yes |  | The selector of the element, multiple formats supported. The matching element is clicked. |
| `modifier` | String | yes |  | The keyboard modifier to press while clicking (e.g., "Shift", "Control", "Alt"). |

Returns: `Unit`

### `click_matches`

```
tab.clickMatches(selector: String, attrName: String, pattern: String, count: Int = 1)
```

This method clicks an element with [selector] whose attribute name is [attrName] and value matches [pattern], and then focuses it.

| Argument | Type | Required | Default | Meaning |
|---|---|---|---|---|
| `selector` | String | yes |  | The selector of the element, multiple formats supported. If there are multiple matching elements, the first will be focused. |
| `attrName` | String | yes |  | The attribute name to match. |
| `pattern` | String | yes |  | The pattern to match the attribute value. |
| `count` | Int | no | 1 | The number of times to click. |

Returns: `Unit`

Examples:

- Example usage:

  ```kotlin
  * driver.clickMatches("button", "type", "submit")
       *
  ```

<details><summary>Full documentation</summary>

This method clicks an element with [selector] whose attribute name is [attrName] and value matches [pattern], and then focuses it.

If there's no element matching [selector], or the element has no attribute [attrName], or the element's attribute value doesn't match [pattern], nothing to do.

```kotlin driver.clickMatches("button", "type", "submit") ```

elements, the first will be focused.

</details>

### `click_text_matches`

```
tab.clickTextMatches(selector: String, pattern: String, count: Int = 1)
```

This method clicks an element with [selector] whose text content matches [pattern], and then focuses it.

| Argument | Type | Required | Default | Meaning |
|---|---|---|---|---|
| `selector` | String | yes |  | The selector of the element, multiple formats supported. If there are multiple matching elements, the first will be focused. |
| `pattern` | String | yes |  | The pattern to match the text content. |
| `count` | Int | no | 1 | The number of times to click. |

Returns: `Unit`

Examples:

- Example usage:

  ```kotlin
  * driver.clickTextMatches("button", "submit")
       *
  ```

<details><summary>Full documentation</summary>

This method clicks an element with [selector] whose text content matches [pattern], and then focuses it.

If there's no element matching [selector], or the element's text content doesn't match [pattern], nothing to do.

```kotlin driver.clickTextMatches("button", "submit") ```

TODO: use playwright style selector which supports text matching, for example: `button:has-text("submit")`

elements, the first will be focused.

</details>

### `clickable_point`

```
tab.clickablePoint(selector: String)
```

Calculate the clickable point of an element located by [selector].

| Argument | Type | Required | Default | Meaning |
|---|---|---|---|---|
| `selector` | String | yes |  | The selector of the element, multiple formats supported. Used to calculate the clickable point. |

Returns: `PointD?`

<details><summary>Full documentation</summary>

Calculate the clickable point of an element located by [selector].

If the element does not exist, or is not clickable, returns null.

</details>

### `console_clear`, `browser_console_clear`

```
tab.consoleClear()
```

Clear the buffered console message list.

Returns: `String`

<details><summary>Full documentation</summary>

tab.consoleClear()

Empties the in-browser console message buffer.

</details>

### `console_messages`, `browser_console_messages`

```
tab.consoleMessages(level: String = info)
```

Retrieve buffered browser console messages (log/warn/error/info/debug).

| Argument | Type | Required | Default | Meaning |
|---|---|---|---|---|
| `level` | String | no | info | level: String = info |

Returns: `String`

<details><summary>Full documentation</summary>

tab.consoleMessages()
tab.consoleMessages(level: String = "info")

Returns a JSON array of console messages filtered to the given minimum level.
Intercepts console.log/warn/error/info/debug on first call and buffers subsequent messages.

</details>

### `current_url`

```
tab.currentUrl()
```

Returns a string representing the current URL that the browser is looking at.

Returns: `String`

<details><summary>Full documentation</summary>

Returns a string representing the current URL that the browser is looking at.

The current url is always the main frame's `document.documentURI` if the browser succeed to return it, and is displayed in the browser's address bar.

If the browser failed to return a proper url, returns the passed in url to navigate, just like a real user enter a url in the address bar but the browser failed to load the page.

NOTE: the url can be non-standard, for example:

about:blank chrome://newtab chrome://settings

see [ai.platon.cdt.kt.protocol.types.page.NavigationEntry.userTypedURL]

</details>

### `dblclick`

```
tab.dblclick(selector: String, modifier: String)
```

Focus on an element with [selector] and double-click it with [modifier] pressed.

| Argument | Type | Required | Default | Meaning |
|---|---|---|---|---|
| `selector` | String | yes |  | The selector of the element, multiple formats supported. The matching element is double-clicked. |
| `modifier` | String | yes |  | The keyboard modifier to press while double-clicking. |

Returns: `Unit`

### `delay`

```
tab.delay(duration: kotlin.time.Duration)
```

Delay for a given amount of time.

| Argument | Type | Required | Default | Meaning |
|---|---|---|---|---|
| `duration` | kotlin.time.Duration | yes |  | The amount of time to delay. |

Returns: `Unit`

### `delete_cookies`

```
tab.deleteCookies(name: String, url: String? = null, domain: String? = null, path: String? = null)
```

Deletes browser cookies with matching name and url or domain/path pair.

| Argument | Type | Required | Default | Meaning |
|---|---|---|---|---|
| `name` | String | yes |  | Name of the cookies to remove. |
| `url` | String? | no | null | If specified, deletes all the cookies with the given name where domain and path match provided URL. |
| `domain` | String? | no | null | If specified, deletes only cookies with the exact domain. |
| `path` | String? | no | null | If specified, deletes only cookies with the exact path. |

Returns: `Unit`

Examples:

- Example usage:

  ```kotlin
  * driver.deleteCookies("name", "https://www.example.com")
       *
  ```

<details><summary>Full documentation</summary>

Deletes browser cookies with matching name and url or domain/path pair.

```kotlin driver.deleteCookies("name", "https://www.example.com") ```

> NOTE: At least one of the url and domain needs to be specified

match provided URL.

</details>

### `dialog_accept`

```
tab.dialogAccept(promptText: String? = null)
```

Accepts the dialog.

| Argument | Type | Required | Default | Meaning |
|---|---|---|---|---|
| `promptText` | String? | no | null | promptText: String? = null |

Returns: `Unit`

### `dialog_dismiss`

```
tab.dialogDismiss()
```

Dismisses the dialog.

Returns: `Unit`

### `document_u_r_i`

```
tab.documentURI()
```

Returns the document location as a string.

Returns: `String`

<details><summary>Full documentation</summary>

Returns the document location as a string.

This property equals to javascript `document.documentURI`.

The `document.URL` property which returns the same value. The `document.documentURI` property can be used on any document types, while the `document.URL` property can only be used on HTML documents.

</details>

### `drag`, `browser_drag`

```
tab.drag(sourceSelector: String, targetSelector: String)
```

Drags the element identified by [sourceSelector] onto the element identified by [targetSelector].

| Argument | Type | Required | Default | Meaning |
|---|---|---|---|---|
| `sourceSelector` | String | yes |  | The selector of the element, multiple formats supported. Identifies the drag source. |
| `targetSelector` | String | yes |  | The selector of the element, multiple formats supported. Identifies the drop target. |

Returns: `Unit`

<details><summary>Full documentation</summary>

Drags the element identified by [sourceSelector] onto the element identified by [targetSelector].

The implementation dispatches an HTML5 drag sequence between the source and target elements using a shared `DataTransfer` payload so selector-to-selector drag flows can be asserted reliably by automation clients.

</details>

### `drag_and_drop`

```
tab.dragAndDrop(selector: String, deltaX: Int, deltaY: Int = 0)
```

Performs a drag, dragenter, dragover, and drop in sequence.

| Argument | Type | Required | Default | Meaning |
|---|---|---|---|---|
| `selector` | String | yes |  | The selector of the element, multiple formats supported. The matching element is dragged from. |
| `deltaX` | Int | yes |  | The distance to drag horizontally. |
| `deltaY` | Int | no | 0 | The distance to drag vertically. |

Returns: `Unit`

### `eval`

```
tab.eval(expression: String)
```

Alias of evaluateValue for page or element-scoped JavaScript evaluation.

| Argument | Type | Required | Default | Meaning |
|---|---|---|---|---|
| `expression` | String | yes |  | expression: String |

Returns: `Any?`

<details><summary>Full documentation</summary>

tab.eval(expression: String)
tab.eval(expression: String, selector: String)

Alias of tab.evaluateValue(...).
When both selector and expression are provided, expression is forwarded as functionDeclaration.

</details>

### `evaluate`

```
tab.evaluate(expression: String)
```

Executes JavaScript in the context of the currently selected frame or window.

| Argument | Type | Required | Default | Meaning |
|---|---|---|---|---|
| `expression` | String | yes |  | JavaScript expression to evaluate |

Returns: `Any?`

Examples:

- Example usage:

  ```kotlin
  * val title = driver.evaluate("document.title")
       *
  ```
- Example usage:

  ```kotlin
  * val code = """
       * () => {
       *   const a = 10;
       *   const b = 20;
       *   return a * b;
       * }
       * """.trimIndent()
       *
       * val result = driver.evaluate(code)
       *
  ```

<details><summary>Full documentation</summary>

Executes JavaScript in the context of the currently selected frame or window.

If the result is not JavaScript object, it is not returned.

If you want to execute a function, convert it to IIFE (Immediately Invoked Function Expression).

```kotlin val title = driver.evaluate("document.title") ```

Multi-line JavaScript code:

```kotlin val code = """ () => { const a = 10; const b = 20; return a * b; } """.trimIndent()

val result = driver.evaluate(code) ```

### 🔍 Notes: * **Wrap the code in an IIFE (Immediately Invoked Function Expression)** to return a value. * **Escape line breaks** with `\n`.

</details>

### `evaluate_detail`

```
tab.evaluateDetail(expression: String)
```

returns detailed evaluation metadata (beta).

| Argument | Type | Required | Default | Meaning |
|---|---|---|---|---|
| `expression` | String | yes |  | The JavaScript expression to evaluate. |

Returns: `JsEvaluation?`

### `evaluate_value`, `browser_evaluate`

```
tab.evaluateValue(selector: String, functionDeclaration: String)
```

Executes JavaScript for the element located by [selector] and returns the result as a JSON-serializable value.

| Argument | Type | Required | Default | Meaning |
|---|---|---|---|---|
| `selector` | String | yes |  | The selector of the element, multiple formats supported. |
| `functionDeclaration` | String | yes |  | The JavaScript function declaration to execute against the matched element. |

Returns: `Any?`

### `evaluate_value_detail`

```
tab.evaluateValueDetail(selector: String, functionDeclaration: String)
```

Returns detailed value evaluation metadata for the element located by [selector].

| Argument | Type | Required | Default | Meaning |
|---|---|---|---|---|
| `selector` | String | yes |  | The selector of the element, multiple formats supported. |
| `functionDeclaration` | String | yes |  | The JavaScript function declaration to execute against the matched element. |

Returns: `JsEvaluation?`

### `execute_cdp_command`

```
tab.executeCdpCommand(method: String, params: Map<String, Any?>? = null)
```

Execute a Chrome DevTools Protocol (CDP) command and return the deserialized result.

| Argument | Type | Required | Default | Meaning |
|---|---|---|---|---|
| `method` | String | yes |  | The full CDP method name (e.g. "Page.captureScreenshot"). |
| `params` | Map<String, Any?>? | no | null | Optional parameters for the CDP command. |

Returns: `Any?`

Examples:

- Example usage:

  ```kotlin
  * // Screenshot the page as JPEG
       * val result = driver.executeCdpCommand("Page.captureScreenshot", mapOf("format" to "jpeg", "quality" to 80))
       *
       * // Get all cookies
       * val cookies = driver.executeCdpCommand("Network.getCookies")
       *
  ```

<details><summary>Full documentation</summary>

Execute a Chrome DevTools Protocol (CDP) command and return the deserialized result.

This is an extension point for advanced scenarios that are not yet covered by the high-level WebDriver API. Use it to issue arbitrary CDP commands directly against the current page target.

The return value is the deserialized CDP response (Map, List, String, Number, Boolean, or null).

Example: ```kotlin // Screenshot the page as JPEG val result = driver.executeCdpCommand("Page.captureScreenshot", mapOf("format" to "jpeg", "quality" to 80))

// Get all cookies val cookies = driver.executeCdpCommand("Network.getCookies") ```

</details>

### `exists`

```
tab.exists(selector: String)
```

Returns whether the element exists.

| Argument | Type | Required | Default | Meaning |
|---|---|---|---|---|
| `selector` | String | yes |  | The selector of the element, multiple formats supported. The element is checked for existence. |

Returns: `Boolean`

Examples:

- Example usage:

  ```kotlin
  * driver.exists("h2.title")
       *
  ```

<details><summary>Full documentation</summary>

Returns whether the element exists.

```kotlin driver.exists("h2.title") ```

</details>

### `fill`, `browser_type`

```
tab.fill(selector: String, text: String)
```

This method emulates inserting text that doesn't come from a key press.

| Argument | Type | Required | Default | Meaning |
|---|---|---|---|---|
| `selector` | String | yes |  | The selector of the element, multiple formats supported. The matching element is focused and then filled with text. If there are multiple matching elements, the first will be focused. |
| `text` | String | yes |  | The text to fill. |

Returns: `Unit`

Examples:

- Example usage:

  ```kotlin
  * driver.fill("input[name='q']", "Hello, World!")
       *
  ```

<details><summary>Full documentation</summary>

This method emulates inserting text that doesn't come from a key press.

Unlike [type], this method clears the existing value before typing.

```kotlin driver.fill("input[name='q']", "Hello, World!") ```

then filled with text. If there are multiple matching elements, the first will be focused.

</details>

### `focus`, `browser_focus`

```
tab.focus(selector: String)
```

This method fetches an element with `selector` and focuses it.

| Argument | Type | Required | Default | Meaning |
|---|---|---|---|---|
| `selector` | String | yes |  | The selector of the element, multiple formats supported. If there are multiple matching elements, the first will be focused. |

Returns: `Unit`

Examples:

- Example usage:

  ```kotlin
  * driver.focus("input[name='q']")
       *
  ```

<details><summary>Full documentation</summary>

This method fetches an element with `selector` and focuses it.

If there's no element matching `selector`, nothing to do.

```kotlin driver.focus("input[name='q']") ```

elements, the first will be focused.

</details>

### `frame_list`, `browser_frame_list`

```
tab.frameList()
```

List the frames of the current page (frame tree, depth-first, main frame first).

Returns: `List<FrameInfo>`

<details><summary>Full documentation</summary>

tab.frameList()

Returns the page's frames with their id, name, url, parent frame id, and depth.
The frame that element operations are currently scoped to is marked active.

</details>

### `frame_main`, `browser_frame_main`

```
tab.frameMain()
```

Switch back to the main frame, undoing the scope set by frameSwitch.

Returns: `Unit`

<details><summary>Full documentation</summary>

tab.frameMain()

All subsequent element operations resolve against the main document again.

</details>

### `frame_switch`, `browser_frame_switch`

```
tab.frameSwitch(frame: String)
```

Switch the frame that subsequent CSS-selector element operations resolve against.

| Argument | Type | Required | Default | Meaning |
|---|---|---|---|---|
| `frame` | String | yes |  | frame: String |

Returns: `Map<String, Any?>`

<details><summary>Full documentation</summary>

tab.frameSwitch(frame: String)

Resolves the target in this order: a CSS selector matching an <iframe> in the
currently scoped document (nested switching works), an exact frame id as printed
by tab.frameList(), an exact frame name, or a case-insensitive url substring.
After switching, click/fill/type/hover/focus/waitForSelector/isVisible and the
element-scoped reads resolve their CSS selectors inside the selected frame.
Return to the main frame with tab.frameMain(); the scope also resets on navigation.

</details>

### `generate_locator`, `browser_generate_locator`

```
tab.generateLocator(selector: String)
```

Generates a unique CSS selector path for the element located by [selector].

| Argument | Type | Required | Default | Meaning |
|---|---|---|---|---|
| `selector` | String | yes |  | CSS selector or element reference (e5, backend:15). |

Returns: `String?`

<details><summary>Full documentation</summary>

Generates a unique CSS selector path for the element located by [selector].

Walks up the DOM from the target element, building a CSS selector segment for each ancestor using the best available strategy: `#id`, `tag.stable-classes`, or `tag:nth-of-type(n)`. The resulting path is joined with `>` separators, producing a concise, human-readable selector suitable for use in other commands.

</details>

### `get_cookies`

```
tab.getCookies()
```

Returns the cookies of the current page.

Returns: `List<Map<String, String>>`

Examples:

- Example usage:

  ```kotlin
  * val cookies = driver.getCookies()
       *
  ```

<details><summary>Full documentation</summary>

Returns the cookies of the current page.

```kotlin val cookies = driver.getCookies() ```

</details>

### `go_back`, `browser_navigate_back`

```
tab.goBack()
```

Navigates the browser to the previous page in the navigation history.

Returns: `Unit`

<details><summary>Full documentation</summary>

Navigates the browser to the previous page in the navigation history.

This method is expected to use the browser's navigation history to move back to the previous page. It should handle any exceptions that may occur during the navigation process.

</details>

### `go_forward`, `browser_navigate_forward`

```
tab.goForward()
```

Navigates the browser to the next page in the navigation history.

Returns: `Unit`

<details><summary>Full documentation</summary>

Navigates the browser to the next page in the navigation history.

This method is expected to use the browser's navigation history to move forward to the next page. It should handle any exceptions that may occur during the navigation process.

</details>

### `har_start`, `browser_har_start`

```
tab.harStart(contentMode: String? = none)
```

Start a HAR recording session on the current tab (bodies captured per content mode: none, text, or all).

| Argument | Type | Required | Default | Meaning |
|---|---|---|---|---|
| `contentMode` | String? | no | none | contentMode: String? = none |

Returns: `Map<String, Any?>`

<details><summary>Full documentation</summary>

tab.harStart(contentMode: String = "none")

Starts recording network traffic into a HAR 1.2 document.
contentMode — which response bodies to embed: "none" (default), "text" (text-like MIME types), or "all" (binary base64).
Stop with tab.harStop() and write the returned "har" document to a .har file.

</details>

### `har_stop`, `browser_har_stop`

```
tab.harStop()
```

Stop the active HAR recording and return the complete HAR 1.2 document.

Returns: `Map<String, Any?>`

<details><summary>Full documentation</summary>

tab.harStop()

Stops recording and returns { recording, contentMode, entries, har } where har is the
HAR 1.2 document. Serialize it to JSON to get a .har file.

</details>

### `hover`, `browser_hover`

```
tab.hover(selector: String)
```

This method hovers over the element.

| Argument | Type | Required | Default | Meaning |
|---|---|---|---|---|
| `selector` | String | yes |  | The selector of the element, multiple formats supported. |

Returns: `Unit`

Examples:

- Example usage:

  ```kotlin
  * driver.hover("h1")
       *
  ```

<details><summary>Full documentation</summary>

This method hovers over the element.

1. Scroll the element into view if needed. 2. Move mouse to hover over a random position near the center of the element.

```kotlin driver.hover("h1") ```

</details>

### `is_checked`, `browser_is_checked`

```
tab.isChecked(selector: String)
```

Returns whether the element is checked.

| Argument | Type | Required | Default | Meaning |
|---|---|---|---|---|
| `selector` | String | yes |  | The selector of the element, multiple formats supported. The element is checked for checked state. |

Returns: `Boolean`

Examples:

- Example usage:

  ```kotlin
  * driver.isChecked("input[name='agree']")
       *
  ```

<details><summary>Full documentation</summary>

Returns whether the element is checked.

```kotlin driver.isChecked("input[name='agree']") ```

</details>

### `is_hidden`

```
tab.isHidden(selector: String)
```

Returns whether the element is hidden.

| Argument | Type | Required | Default | Meaning |
|---|---|---|---|---|
| `selector` | String | yes |  | The selector of the element, multiple formats supported. The element is checked for visibility state. |

Returns: `Boolean`

Examples:

- Example usage:

  ```kotlin
  * driver.isHidden("input[name='q']")
       *
  ```

<details><summary>Full documentation</summary>

Returns whether the element is hidden.

```kotlin driver.isHidden("input[name='q']") ```

</details>

### `is_visible`, `browser_is_visible`

```
tab.isVisible(selector: String)
```

Returns whether the element is visible.

| Argument | Type | Required | Default | Meaning |
|---|---|---|---|---|
| `selector` | String | yes |  | The selector of the element, multiple formats supported. The element is checked for visibility state. |

Returns: `Boolean`

Examples:

- Example usage:

  ```kotlin
  * driver.isVisible("input[name='q']")
       *
  ```

<details><summary>Full documentation</summary>

Returns whether the element is visible.

```kotlin driver.isVisible("input[name='q']") ```

</details>

### `key_down`, `browser_keydown`

```
tab.keyDown(key: String)
```

Presses and holds a keyboard key on the currently focused element.

| Argument | Type | Required | Default | Meaning |
|---|---|---|---|---|
| `key` | String | yes |  | A key to press and hold. The key can be a single character, a key name, or a combination of both. See [Code values for keyboard events](https://developer.mozilla.org/en-US/docs/Web/API/UI_Events/Keyboard_event_code_values) |

Returns: `Unit`

Examples:

- Example usage:

  ```kotlin
  * driver.keyDown("Shift")
       * driver.keyDown("Control")
       *
  ```

<details><summary>Full documentation</summary>

Presses and holds a keyboard key on the currently focused element.

```kotlin driver.keyDown("Shift") driver.keyDown("Control") ```

See [Code values for keyboard events](https://developer.mozilla.org/en-US/docs/Web/API/UI_Events/Keyboard_event_code_values)

</details>

### `key_up`, `browser_keyup`

```
tab.keyUp(key: String)
```

Releases a previously pressed keyboard key on the currently focused element.

| Argument | Type | Required | Default | Meaning |
|---|---|---|---|---|
| `key` | String | yes |  | A key to release. The key can be a single character, a key name, or a combination of both. See [Code values for keyboard events](https://developer.mozilla.org/en-US/docs/Web/API/UI_Events/Keyboard_event_code_values) |

Returns: `Unit`

Examples:

- Example usage:

  ```kotlin
  * driver.keyUp("Shift")
       * driver.keyUp("Control")
       *
  ```

<details><summary>Full documentation</summary>

Releases a previously pressed keyboard key on the currently focused element.

```kotlin driver.keyUp("Shift") driver.keyUp("Control") ```

See [Code values for keyboard events](https://developer.mozilla.org/en-US/docs/Web/API/UI_Events/Keyboard_event_code_values)

</details>

### `load_jsoup_resource`

```
tab.loadJsoupResource(url: String)
```

Load the url as a resource with Jsoup rather than browser rendering, with the last page's context.

| Argument | Type | Required | Default | Meaning |
|---|---|---|---|---|
| `url` | String | yes |  | The URL to load. |

Returns: `Connection.Response`

<details><summary>Full documentation</summary>

Load the url as a resource with Jsoup rather than browser rendering, with the last page's context.

This means, the same headers and cookies.

</details>

### `load_resource`

```
tab.loadResource(url: String)
```

Load the url as a resource without browser rendering, with the last page's context.

| Argument | Type | Required | Default | Meaning |
|---|---|---|---|---|
| `url` | String | yes |  | The URL to load. |

Returns: `NetworkResourceResponse`

<details><summary>Full documentation</summary>

Load the url as a resource without browser rendering, with the last page's context.

This means, the same headers and cookies.

</details>

### `load_storage_state`, `browser_load_storage_state`

```
tab.loadStorageState(state: String)
```

Load a JSON storage-state payload, restoring cookies and localStorage.

| Argument | Type | Required | Default | Meaning |
|---|---|---|---|---|
| `state` | String | yes |  | state: String |

Returns: `String`

<details><summary>Full documentation</summary>

tab.loadStorageState(state: String)

Restores cookies plus localStorage from a JSON string previously returned by tab.saveStorageState().

</details>

### `mouse_down`, `browser_mouse_down`

```
tab.mouseDown(button: String = left, clickCount: Int = 1)
```

Presses a mouse button at the current mouse position.

| Argument | Type | Required | Default | Meaning |
|---|---|---|---|---|
| `button` | String | no | left | The mouse button to press. Supported values are `left`, `middle`, and `right`. |
| `clickCount` | Int | no | 1 | The click count associated with the press event. |

Returns: `Unit`

Examples:

- Example usage:

  ```kotlin
  * driver.mouseDown()
       * driver.mouseDown("right")
       *
  ```

<details><summary>Full documentation</summary>

Presses a mouse button at the current mouse position.

```kotlin driver.mouseDown() driver.mouseDown("right") ```

</details>

### `mouse_move`, `browser_mouse_move_xy`

```
tab.mouseMove(x: Double, y: Double)
```

Moves the mouse to the position specified by [x] and [y].

| Argument | Type | Required | Default | Meaning |
|---|---|---|---|---|
| `x` | Double | yes |  | The x coordinate to move to. |
| `y` | Double | yes |  | The y coordinate to move to. |

Returns: `Unit`

Examples:

- Example usage:

  ```kotlin
  * driver.mouseMove(100.0, 200.0)
       *
  ```

<details><summary>Full documentation</summary>

Moves the mouse to the position specified by [x] and [y].

```kotlin driver.mouseMove(100.0, 200.0) ```

</details>

### `mouse_up`, `browser_mouse_up`

```
tab.mouseUp(button: String = left, clickCount: Int = 1)
```

Releases a mouse button at the current mouse position.

| Argument | Type | Required | Default | Meaning |
|---|---|---|---|---|
| `button` | String | no | left | The mouse button to release. Supported values are `left`, `middle`, and `right`. |
| `clickCount` | Int | no | 1 | The click count associated with the release event. |

Returns: `Unit`

Examples:

- Example usage:

  ```kotlin
  * driver.mouseUp()
       * driver.mouseUp("right")
       *
  ```

<details><summary>Full documentation</summary>

Releases a mouse button at the current mouse position.

```kotlin driver.mouseUp() driver.mouseUp("right") ```

</details>

### `mouse_wheel`, `browser_mouse_wheel`

```
tab.mouseWheel(selector: String, deltaX: Double = 0.0, deltaY: Double = 150.0)
```

Scrolls the mouse wheel on a specific element identified by [selector].

| Argument | Type | Required | Default | Meaning |
|---|---|---|---|---|
| `selector` | String | yes |  | The selector of the element to wheel on. |
| `deltaX` | Double | no | 0.0 | The distance to wheel horizontally. |
| `deltaY` | Double | no | 150.0 | The distance to wheel vertically. |

Returns: `Unit`

Examples:

- Example usage:

  ```kotlin
  * driver.mouseWheel(".scrollable-panel", 0.0, 200.0)
       *
  ```

<details><summary>Full documentation</summary>

Scrolls the mouse wheel on a specific element identified by [selector].

The element is scrolled into view if needed, the mouse is moved to a random position near the element's center, and then the wheel event is dispatched.

```kotlin driver.mouseWheel(".scrollable-panel", 0.0, 200.0) ```

</details>

### `mouse_wheel_down`

```
tab.mouseWheelDown(count: Int = 1, deltaX: Double = 0.0, deltaY: Double = 150.0, delayMillis: Long = -1)
```

The mouse wheels down for [count] times.

| Argument | Type | Required | Default | Meaning |
|---|---|---|---|---|
| `count` | Int | no | 1 | The times to wheel down. |
| `deltaX` | Double | no | 0.0 | The distance to wheel horizontally. |
| `deltaY` | Double | no | 150.0 | The distance to wheel vertically. |
| `delayMillis` | Long | no | -1 | The delay time in milliseconds between ticks. Use 0 for no delay, -1 (default) for the policy-based random delay, or a positive value for a fixed delay. |

Returns: `Unit`

Examples:

- Example usage:

  ```kotlin
  * driver.mouseWheelDown(3)
       *
  ```

<details><summary>Full documentation</summary>

The mouse wheels down for [count] times.

```kotlin driver.mouseWheelDown(3) ```

Use 0 for no delay, -1 (default) for the policy-based random delay, or a positive value for a fixed delay.

</details>

### `mouse_wheel_up`

```
tab.mouseWheelUp(count: Int = 1, deltaX: Double = 0.0, deltaY: Double = -150.0, delayMillis: Long = -1)
```

The mouse wheels up for [count] times.

| Argument | Type | Required | Default | Meaning |
|---|---|---|---|---|
| `count` | Int | no | 1 | The times to wheel up. |
| `deltaX` | Double | no | 0.0 | The distance to wheel horizontally. |
| `deltaY` | Double | no | -150.0 | The distance to wheel vertically. |
| `delayMillis` | Long | no | -1 | The delay time in milliseconds between ticks. Use 0 for no delay, -1 (default) for the policy-based random delay, or a positive value for a fixed delay. |

Returns: `Unit`

Examples:

- Example usage:

  ```kotlin
  * driver.mouseWheelUp(3)
       *
  ```

<details><summary>Full documentation</summary>

The mouse wheels up for [count] times.

```kotlin driver.mouseWheelUp(3) ```

Use 0 for no delay, -1 (default) for the policy-based random delay, or a positive value for a fixed delay.

</details>

### `move_mouse_to`

```
tab.moveMouseTo(selector: String, deltaX: Int, deltaY: Int = 0)
```

The mouse moves to the element with [selector].

| Argument | Type | Required | Default | Meaning |
|---|---|---|---|---|
| `selector` | String | yes |  | The selector of the element, multiple formats supported. |
| `deltaX` | Int | yes |  | The distance to the left of the element. |
| `deltaY` | Int | no | 0 | The distance to the top of the element. |

Returns: `Unit`

Examples:

- Example usage:

  ```kotlin
  * driver.moveMouseTo("h2.title")
       *
  ```

<details><summary>Full documentation</summary>

The mouse moves to the element with [selector].

```kotlin driver.moveMouseTo("h2.title") ```

</details>

### `nano_d_o_m_tree`

```
tab.nanoDOMTree()
```

Retrieves a lightweight version of the DOM tree (NanoDOMTree).

Returns: `NanoDOMTree?`

<details><summary>Full documentation</summary>

Retrieves a lightweight version of the DOM tree (NanoDOMTree).

The NanoDOMTree is based on the accessibility tree and enhanced with DOM and document snapshot information. It is designed to be a compact representation suitable for AI processing and analysis.

</details>

### `navigate`, `browser_navigate`

```
tab.navigate(entry: NavigateEntry)
```

Navigates current page to the given URL.

| Argument | Type | Required | Default | Meaning |
|---|---|---|---|---|
| `entry` | NavigateEntry | yes |  | NavigateEntry to navigate page to. |

Returns: `Unit`

Examples:

- Example usage:

  ```kotlin
  * val entry = NavigateEntry("https://www.example.com?timestamp=11712067353", pageUrl = "https://www.example.com")
       * driver.navigate(entry)
       * driver.waitForNavigation()
       *
  ```

<details><summary>Full documentation</summary>

Navigates current page to the given URL.

```kotlin val entry = NavigateEntry("https://www.example.com?timestamp=11712067353", pageUrl = "https://www.example.com") driver.navigate(entry) driver.waitForNavigation() ```

</details>

### `network_request_detail`, `browser_network_request`

```
tab.networkRequestDetail(requestId: String)
```

Fetch the full detail of one tracked network request, including headers, timing, and response body.

| Argument | Type | Required | Default | Meaning |
|---|---|---|---|---|
| `requestId` | String | yes |  | requestId: String |

Returns: `Map<String, Any?>`

<details><summary>Full documentation</summary>

tab.networkRequestDetail(requestId: String)

Returns request/response metadata plus the response body (fetched on demand) for the
request id shown by tab.networkRequests().

</details>

### `network_requests`, `browser_network_requests`

```
tab.networkRequests(filter: String? = null, type: String? = null, method: String? = null, status: String? = null, clear: Boolean = false)
```

List network requests tracked for the current tab, optionally filtered.

| Argument | Type | Required | Default | Meaning |
|---|---|---|---|---|
| `filter` | String? | no | null | filter: String? = null |
| `type` | String? | no | null | type: String? = null |
| `method` | String? | no | null | method: String? = null |
| `status` | String? | no | null | status: String? = null |
| `clear` | Boolean | no | false | clear: Boolean = false |

Returns: `List<TrackedNetworkRequest>`

<details><summary>Full documentation</summary>

tab.networkRequests()
tab.networkRequests(filter: String? = null, type: String? = null, method: String? = null, status: String? = null, clear: Boolean = false)

Enables Network tracking on first use, then returns the requests observed so far.
filter — only requests whose URL contains this text (case-insensitive).
type — comma-separated CDP resource types, e.g. "xhr,fetch".
method — HTTP method, e.g. "POST".
status — "200", "2xx", or a range like "400-499".
clear — drop all tracked requests first.

</details>

### `network_route`, `browser_network_route`

```
tab.networkRoute(urlPattern: String, abort: Boolean = false, body: String? = null, contentType: String? = null, resourceType: String? = null)
```

Route matching requests to a mock response or abort them (CDP Fetch interception).

| Argument | Type | Required | Default | Meaning |
|---|---|---|---|---|
| `urlPattern` | String | yes |  | urlPattern: String |
| `abort` | Boolean | no | false | abort: Boolean = false |
| `body` | String? | no | null | body: String? = null |
| `contentType` | String? | no | null | contentType: String? = null |
| `resourceType` | String? | no | null | resourceType: String? = null |

Returns: `Map<String, Any?>`

<details><summary>Full documentation</summary>

tab.networkRoute(urlPattern: String, abort: Boolean = false, body: String? = null, contentType: String? = null, resourceType: String? = null)

Intercepts requests whose URL matches urlPattern ("*" matches all; plain text matches
URLs containing it; "*" globs like "**/api/users" are supported).
abort — fail matching requests instead of sending them.
body/contentType — answer matching requests with this mock response.
resourceType — only intercept these CDP resource types (comma-separated, e.g. "xhr,fetch").
Remove routes with tab.networkUnroute().

</details>

### `network_unroute`, `browser_network_unroute`

```
tab.networkUnroute(urlPattern: String? = null)
```

Remove request routes; without a pattern every route is removed and Fetch interception is disabled.

| Argument | Type | Required | Default | Meaning |
|---|---|---|---|---|
| `urlPattern` | String? | no | null | urlPattern: String? = null |

Returns: `Map<String, Any?>`

<details><summary>Full documentation</summary>

tab.networkUnroute(urlPattern: String? = null)

Removes the route registered with the exact urlPattern, or all routes (and disables
Fetch interception) when no pattern is given.

</details>

### `new_jsoup_session`

```
tab.newJsoupSession()
```

Create a new Jsoup session with the last page's context, which means, the same headers and cookies.

Returns: `Connection`

### `open`

```
tab.open(url: String)
```

Opens the specified URL in the web driver.

| Argument | Type | Required | Default | Meaning |
|---|---|---|---|---|
| `url` | String | yes |  | The URL to which the web driver should navigate. Must be a valid URL string. |

Returns: `Unit`

Examples:

- Example usage:

  ```kotlin
  * driver.open("https://www.example.com")
       *
  ```

<details><summary>Full documentation</summary>

Opens the specified URL in the web driver.

This function navigates the web driver to the provided URL and waits for the navigation to complete. It is a suspend function, meaning it can be used within coroutines for asynchronous execution.

Example usage: ```kotlin driver.open("https://www.example.com") ```

</details>

### `outer_h_t_m_l`

```
tab.outerHTML(selector: String)
```

Returns the node's HTML markup, the node is located by [selector].

| Argument | Type | Required | Default | Meaning |
|---|---|---|---|---|
| `selector` | String | yes |  | The selector of the element, multiple formats supported. |

Returns: `String?`

Examples:

- Example usage:

  ```kotlin
  * val html = driver.outerHTML("h2.title")
       *
  ```

<details><summary>Full documentation</summary>

Returns the node's HTML markup, the node is located by [selector].

If the node does not exist, returns null.

```kotlin val html = driver.outerHTML("h2.title") ```

</details>

### `page_source`

```
tab.pageSource()
```

Returns the source of the last loaded page.

Returns: `String?`

Examples:

- Example usage:

  ```kotlin
  * val pageSource = driver.pageSource()
       *
  ```

<details><summary>Full documentation</summary>

Returns the source of the last loaded page.

If the page has been modified after loading (for example, by JavaScript) there is no guarantee that the returned text is that of the modified page.

PageSource and outerHTML:

- pageSource: returns document HTML markup, will support non-HTML document - outerHTML: returns document HTML markup, for HTML document only

```kotlin val pageSource = driver.pageSource() ```

</details>

### `pause`

```
tab.pause()
```

Force the page pauses all navigations and PENDING resource fetches.

Returns: `Unit`

<details><summary>Full documentation</summary>

Force the page pauses all navigations and PENDING resource fetches.

If the page loading pauses, the user can still interact with the page, and therefore resources can continue to load.

</details>

### `pdf`, `browser_pdf_save`

```
tab.pdf()
```

Print the current page as PDF.

Returns: `String?`

<details><summary>Full documentation</summary>

Print the current page as PDF.

Returns a Base64 encoded PDF document generated via Chrome DevTools Protocol Page.printToPDF. Uses A4 paper size, portrait orientation, and prints background graphics by default.

</details>

### `press`, `browser_press_key`

```
tab.press(key: String, selector: String? = null)
```

Press a key on the currently focused element or the element matched by selector.

| Argument | Type | Required | Default | Meaning |
|---|---|---|---|---|
| `key` | String | yes |  | key: String |
| `selector` | String? | no | null | selector: String? = null |

Returns: `Unit`

<details><summary>Full documentation</summary>

tab.press(key: String)
tab.press(key: String, selector: String?)

Presses the key on the currently focused element when selector is omitted.
When selector is provided, the executor focuses the matched element first and then presses the key.

</details>

### `referrer`

```
tab.referrer()
```

The referrer property returns the URI of the page that linked to this page.

Returns: `String`

<details><summary>Full documentation</summary>

The referrer property returns the URI of the page that linked to this page.

The value is an empty string if the user navigated to the page directly (not through a link, but, for example, by using a bookmark).

Inside an <iframe>, the referrer will initially be set to the same value as the href of the parent window's Window.location.

</details>

### `reload`, `browser_reload`

```
tab.reload()
```

Reloads the current page.

Returns: `Unit`

<details><summary>Full documentation</summary>

Reloads the current page.

This method should trigger a reload of the current page, similar to how a user would refresh the page in a browser.

</details>

### `resize`, `browser_resize`

```
tab.resize(width: Int, height: Int)
```

Resizes the viewport to the specified width and height.

| Argument | Type | Required | Default | Meaning |
|---|---|---|---|---|
| `width` | Int | yes |  | width: Int |
| `height` | Int | yes |  | height: Int |

Returns: `Unit`

### `save_storage_state`, `browser_save_storage_state`

```
tab.saveStorageState()
```

Save cookies and the active origin's localStorage as a JSON storage-state payload.

Returns: `String`

<details><summary>Full documentation</summary>

tab.saveStorageState()

Returns a JSON string containing the current browser cookies and the active origin's localStorage entries.

</details>

### `screenshot`, `browser_take_screenshot`

```
tab.screenshot(rect: RectD)
```

Take a screenshot of the rectangle specified by [rect] in the current page coordinate space.

| Argument | Type | Required | Default | Meaning |
|---|---|---|---|---|
| `rect` | RectD | yes |  | rect: RectD |

Returns: `String?`

<details><summary>Full documentation</summary>

Take a screenshot of the rectangle specified by [rect] in the current page coordinate space.

Caller is responsible for ensuring the rectangle is visible or scrolled into view if the implementation requires it.

</details>

### `scroll_by`

```
tab.scrollBy(pixels: Double = 200.0, smooth: Boolean = true)
```

Scrolls the current page frame vertically by the given number of pixels.

| Argument | Type | Required | Default | Meaning |
|---|---|---|---|---|
| `pixels` | Double | no | 200.0 | The delta in pixels to scroll; positive scrolls down, negative scrolls up. Default 200.0. |
| `smooth` | Boolean | no | true | Whether to perform a deterministic smooth scrolling sequence. If false a single `window.scrollTo()` is executed. |

Returns: `Double`

Examples:

- Example usage:

  ```kotlin
  * // Scroll down 200px smoothly (default)
       * driver.scrollBy()
       * // Scroll up 500px immediately
       * driver.scrollBy(-500.0, smooth = false)
       * // Scroll down one viewport height, smooth
       * val y = driver.scrollBy(driver.viewportHeight(), smooth = true)
       *
  ```

<details><summary>Full documentation</summary>

Scrolls the current page frame vertically by the given number of pixels.

Positive values scroll down, negative values scroll up. The target position is clamped to the scrollable range [0, scrollHeight - viewportHeight].

When [smooth] is true a deterministic, step‑wise scrolling algorithm is used (instead of relying on the browser's native smooth scrolling) for higher stability: it issues incremental `window.scrollTo()` calls and waits for the viewport to settle after each step. This reduces race conditions with lazy‑loaded content or async layout changes and yields more predictable final positions.

Typical usage: ```kotlin // Scroll down 200px smoothly (default) driver.scrollBy() // Scroll up 500px immediately driver.scrollBy(-500.0, smooth = false) // Scroll down one viewport height, smooth val y = driver.scrollBy(driver.viewportHeight(), smooth = true) ```

`window.scrollTo()` is executed.

</details>

### `scroll_down`

```
tab.scrollDown(count: Int = 1)
```

The current page frame scrolls down for [count] times.

| Argument | Type | Required | Default | Meaning |
|---|---|---|---|---|
| `count` | Int | no | 1 | The times to scroll down. |

Returns: `Double`

Examples:

- Example usage:

  ```kotlin
  * driver.scrollDown(3)
       *
  ```

<details><summary>Full documentation</summary>

The current page frame scrolls down for [count] times.

```kotlin driver.scrollDown(3) ```

NOTE: This uses JS scrollBy for fast, direct scroll control. For realistic user-like scrolling that triggers all event handlers, use [mouseWheelDown].

</details>

### `scroll_to`

```
tab.scrollTo(selector: String)
```

This method fetches an element with [selector], scrolls it into view if needed.

| Argument | Type | Required | Default | Meaning |
|---|---|---|---|---|
| `selector` | String | yes |  | The selector of the element, multiple formats supported. If there are multiple matching elements, the first will be scrolled into view. |

Returns: `Double`

Examples:

- Example usage:

  ```kotlin
  * driver.scrollTo("h2.title")
       *
  ```

<details><summary>Full documentation</summary>

This method fetches an element with [selector], scrolls it into view if needed.

If there's no element matching [selector], the method does nothing.

```kotlin driver.scrollTo("h2.title") ```

elements, the first will be scrolled into view.

</details>

### `scroll_to_bottom`

```
tab.scrollToBottom()
```

The current page frame scrolls to the bottom.

Returns: `Double`

Examples:

- Example usage:

  ```kotlin
  * driver.scrollToBottom()
       *
  ```

<details><summary>Full documentation</summary>

The current page frame scrolls to the bottom.

```kotlin driver.scrollToBottom() ```

</details>

### `scroll_to_middle`

```
tab.scrollToMiddle(ratio: Double)
```

The current page frame scrolls to the middle.

| Argument | Type | Required | Default | Meaning |
|---|---|---|---|---|
| `ratio` | Double | yes |  | The ratio of the page to scroll to, 0.0 means the top, 1.0 means the bottom. |

Returns: `Double`

Examples:

- Example usage:

  ```kotlin
  * driver.scrollToMiddle(0.2)
       * driver.scrollToMiddle(0.5)
       * driver.scrollToMiddle(0.8)
       *
  ```

<details><summary>Full documentation</summary>

The current page frame scrolls to the middle.

```kotlin driver.scrollToMiddle(0.2) driver.scrollToMiddle(0.5) driver.scrollToMiddle(0.8) ```

</details>

### `scroll_to_top`

```
tab.scrollToTop()
```

The current page frame scrolls to the top.

Returns: `Double`

Examples:

- Example usage:

  ```kotlin
  * driver.scrollToTop()
       *
  ```

<details><summary>Full documentation</summary>

The current page frame scrolls to the top.

```kotlin driver.scrollToTop() ```

</details>

### `scroll_to_viewport`

```
tab.scrollToViewport(n: Double, smooth: Boolean = true)
```

Scroll to the given 0-based viewport position.

| Argument | Type | Required | Default | Meaning |
|---|---|---|---|---|
| `n` | Double | yes |  | The viewport number of the page to scroll to (0-based). 0.00 means at the top of the first screen, 1.50 means halfway through the second screen. |
| `smooth` | Boolean | no | true | smooth: Boolean = true |

Returns: `Double`

Examples:

- Example usage:

  ```kotlin
  * driver.scrollToViewport(0.0)
       * driver.scrollToViewport(0.5)
       * driver.scrollToViewport(1.5)
       * driver.scrollToViewport(2.0)
       *
  ```

<details><summary>Full documentation</summary>

Scroll to the given 0-based viewport position.

```kotlin driver.scrollToViewport(0.0) driver.scrollToViewport(0.5) driver.scrollToViewport(1.5) driver.scrollToViewport(2.0) ```

0.00 means at the top of the first screen, 1.50 means halfway through the second screen.

</details>

### `scroll_up`

```
tab.scrollUp(count: Int = 1)
```

The current page frame scrolls up for [count] times.

| Argument | Type | Required | Default | Meaning |
|---|---|---|---|---|
| `count` | Int | no | 1 | The times to scroll up. |

Returns: `Double`

Examples:

- Example usage:

  ```kotlin
  * driver.scrollUp(3)
       *
  ```

<details><summary>Full documentation</summary>

The current page frame scrolls up for [count] times.

```kotlin driver.scrollUp(3) ```

NOTE: This uses JS scrollBy for fast, direct scroll control. For realistic user-like scrolling that triggers all event handlers, use [mouseWheelUp].

</details>

### `select_attribute_all`

```
tab.selectAttributeAll(selector: String, attrName: String, start: Int = 0, limit: Int = 10000)
```

Returns the nodes' attribute values, the nodes are located by [selector], the attribute is [attrName].

| Argument | Type | Required | Default | Meaning |
|---|---|---|---|---|
| `selector` | String | yes |  | The selector of the element, multiple formats supported. |
| `attrName` | String | yes |  | The attribute name to retrieve. |
| `start` | Int | no | 0 | The offset of the first node to select. |
| `limit` | Int | no | 10000 | The maximum number of nodes to select. |

Returns: `List<String>`

Examples:

- Example usage:

  ```kotlin
  * val classes = driver.selectAttributeAll("h2.title", "class")
       *
  ```

<details><summary>Full documentation</summary>

Returns the nodes' attribute values, the nodes are located by [selector], the attribute is [attrName].

If the nodes do not exist, or the attribute does not exist, returns an empty list.

```kotlin val classes = driver.selectAttributeAll("h2.title", "class") ```

</details>

### `select_attributes`

```
tab.selectAttributes(selector: String)
```

Returns the node's attribute values, the node is located by [selector].

| Argument | Type | Required | Default | Meaning |
|---|---|---|---|---|
| `selector` | String | yes |  | The selector of the element, multiple formats supported. |

Returns: `Map<String, String>`

Examples:

- Example usage:

  ```kotlin
  * val classes = driver.selectAttributes("h2.title", "class")
       *
  ```

<details><summary>Full documentation</summary>

Returns the node's attribute values, the node is located by [selector].

If the node do not exist, or the attribute does not exist, returns an empty list.

```kotlin val classes = driver.selectAttributes("h2.title", "class") ```

</details>

### `select_first_attribute_or_null`

```
tab.selectFirstAttributeOrNull(selector: String, attrName: String)
```

Returns the node's attribute value, the node is located by [selector], the attribute is [attrName].

| Argument | Type | Required | Default | Meaning |
|---|---|---|---|---|
| `selector` | String | yes |  | The selector of the element, multiple formats supported. |
| `attrName` | String | yes |  | The attribute name to retrieve. |

Returns: `String?`

Examples:

- Example usage:

  ```kotlin
  * val classes = driver.selectFirstAttributeOrNull("h2.title", "class")
       *
  ```

<details><summary>Full documentation</summary>

Returns the node's attribute value, the node is located by [selector], the attribute is [attrName].

If the node does not exist, or the attribute does not exist, returns null.

```kotlin val classes = driver.selectFirstAttributeOrNull("h2.title", "class") ```

</details>

### `select_first_property_value_or_null`

```
tab.selectFirstPropertyValueOrNull(selector: String, propName: String)
```

Returns the node's property value, the node is located by [selector], the property is [propName].

| Argument | Type | Required | Default | Meaning |
|---|---|---|---|---|
| `selector` | String | yes |  | The selector of the element, multiple formats supported. |
| `propName` | String | yes |  | The property name to retrieve. |

Returns: `String?`

<details><summary>Full documentation</summary>

Returns the node's property value, the node is located by [selector], the property is [propName].

If the node does not exist, or the property does not exist, returns null.

```kotlin val classes = driver.selectFirstPropertyOrNull("input#input", "value") ```

</details>

### `select_first_text_or_null`

```
tab.selectFirstTextOrNull(selector: String)
```

Returns the node's text content, the node is located by [selector].

| Argument | Type | Required | Default | Meaning |
|---|---|---|---|---|
| `selector` | String | yes |  | The selector of the element, multiple formats supported. |

Returns: `String?`

Examples:

- Example usage:

  ```kotlin
  * val text = driver.selectFirstTextOrNull("h2.title")
       *
  ```

<details><summary>Full documentation</summary>

Returns the node's text content, the node is located by [selector].

If the node does not exist, returns null.

```kotlin val text = driver.selectFirstTextOrNull("h2.title") ```

</details>

### `select_option`, `browser_select_option`

```
tab.selectOption(selector: String, value: String)
```

Selects an option in a <select> element.

| Argument | Type | Required | Default | Meaning |
|---|---|---|---|---|
| `selector` | String | yes |  | The selector of the element, multiple formats supported. |
| `value` | String | yes |  | The value or label of the option to select |

Returns: `String?`

### `select_property_value_all`

```
tab.selectPropertyValueAll(selector: String, propName: String, start: Int = 0, limit: Int = 10000)
```

Returns the nodes' property values, the nodes are located by [selector], the property is [propName].

| Argument | Type | Required | Default | Meaning |
|---|---|---|---|---|
| `selector` | String | yes |  | The selector of the element, multiple formats supported. |
| `propName` | String | yes |  | The property name to retrieve. |
| `start` | Int | no | 0 | The offset of the first node to select. |
| `limit` | Int | no | 10000 | The maximum number of nodes to select. |

Returns: `List<String>`

<details><summary>Full documentation</summary>

Returns the nodes' property values, the nodes are located by [selector], the property is [propName].

If the nodes do not exist, or the property does not exist, returns an empty list.

```kotlin val classes = driver.selectPropertyAll("input#input", "value") ```

</details>

### `select_text_all`

```
tab.selectTextAll(selector: String)
```

Returns a list of text contents of all the elements matching the specified selector within the page.

| Argument | Type | Required | Default | Meaning |
|---|---|---|---|---|
| `selector` | String | yes |  | The selector of the element, multiple formats supported. |

Returns: `List<String>`

Examples:

- Example usage:

  ```kotlin
  * val texts = driver.selectTextAll("h2")
       *
  ```

<details><summary>Full documentation</summary>

Returns a list of text contents of all the elements matching the specified selector within the page.

If no elements match the selector, returns an empty list.

```kotlin val texts = driver.selectTextAll("h2") ```

</details>

### `set_attribute`

```
tab.setAttribute(selector: String, attrName: String, attrValue: String)
```

Set the attribute of an element located by [selector].

| Argument | Type | Required | Default | Meaning |
|---|---|---|---|---|
| `selector` | String | yes |  | The selector of the element, multiple formats supported. |
| `attrName` | String | yes |  | The attribute name to set. |
| `attrValue` | String | yes |  | The attribute value to set. |

Returns: `Unit`

Examples:

- Example usage:

  ```kotlin
  * driver.setAttribute("h2.title", "class", "header")
       *
  ```

<details><summary>Full documentation</summary>

Set the attribute of an element located by [selector].

```kotlin driver.setAttribute("h2.title", "class", "header") ```

</details>

### `set_attribute_all`

```
tab.setAttributeAll(selector: String, attrName: String, attrValue: String)
```

Set the attribute of all elements matching [selector].

| Argument | Type | Required | Default | Meaning |
|---|---|---|---|---|
| `selector` | String | yes |  | The selector of the element, multiple formats supported. |
| `attrName` | String | yes |  | The attribute name to set. |
| `attrValue` | String | yes |  | The attribute value to set. |

Returns: `Unit`

Examples:

- Example usage:

  ```kotlin
  * driver.setAttributeAll("h2.title", "class", "header")
       *
  ```

<details><summary>Full documentation</summary>

Set the attribute of all elements matching [selector].

```kotlin driver.setAttributeAll("h2.title", "class", "header") ```

</details>

### `set_property`

```
tab.setProperty(selector: String, propName: String, propValue: String)
```

Set the property of an element located by [selector].

| Argument | Type | Required | Default | Meaning |
|---|---|---|---|---|
| `selector` | String | yes |  | The selector of the element, multiple formats supported. |
| `propName` | String | yes |  | The property name to set. |
| `propValue` | String | yes |  | The property value to set. |

Returns: `Unit`

Examples:

- Example usage:

  ```kotlin
  * driver.setProperty("input#input", "value")
       *
  ```

<details><summary>Full documentation</summary>

Set the property of an element located by [selector].

```kotlin driver.setProperty("input#input", "value") ```

</details>

### `set_property_all`

```
tab.setPropertyAll(selector: String, propName: String, propValue: String)
```

Set the property of all elements matching [selector].

| Argument | Type | Required | Default | Meaning |
|---|---|---|---|---|
| `selector` | String | yes |  | The selector of the element, multiple formats supported. |
| `propName` | String | yes |  | The property name to set. |
| `propValue` | String | yes |  | The property value to set. |

Returns: `Unit`

Examples:

- Example usage:

  ```kotlin
  * driver.setPropertyAll("input#input", "value")
       *
  ```

<details><summary>Full documentation</summary>

Set the property of all elements matching [selector].

```kotlin driver.setPropertyAll("input#input", "value") ```

</details>

### `stop`

```
tab.stop()
```

Force the page stop all navigations and RELEASES all resources.

Returns: `Unit`

<details><summary>Full documentation</summary>

Force the page stop all navigations and RELEASES all resources.

Interaction with the stop page results in undefined behavior and the results should not be trusted.

If a web driver stops, it can later be used to visit other pages.

</details>

### `text_content`

```
tab.textContent(selector: String? = null)
```

Returns the text content of the document or a specific element.

| Argument | Type | Required | Default | Meaning |
|---|---|---|---|---|
| `selector` | String? | no | null | The selector of the element, multiple formats supported. If null, targets the document body. |

Returns: `String?`

Examples:

- Example usage:

  ```kotlin
  * val bodyText = driver.textContent()
       * val titleText = driver.textContent("h1.title")
       *
  ```

<details><summary>Full documentation</summary>

Returns the text content of the document or a specific element.

If [selector] is null, returns the text content of the entire document (usually `document.body.innerText`). If [selector] is provided, returns the text content of the first matching element.

If the document or element does not exist, returns null.

```kotlin val bodyText = driver.textContent() val titleText = driver.textContent("h1.title") ```

</details>

### `title`

```
tab.title()
```

Returns the title of the current page.

Returns: `String`

### `type`, `browser_press_sequentially`

```
tab.type(text: String, selector: String? = null, method: String? = auto, verify: Boolean = false)
```

Insert text into the currently focused element or the element matched by selector. method=auto|chars|exec: auto (default) uses per-character typing for short text and one execCommand('insertText') bulk insert for long (>150 chars) or multi-line text; chars forces per-character typing; exec forces the bulk insert (selector required). verify=true reads the element back after typing and fails when the content does not match (never silently lose text).

| Argument | Type | Required | Default | Meaning |
|---|---|---|---|---|
| `text` | String | yes |  | text: String |
| `selector` | String? | no | null | selector: String? = null |
| `method` | String? | no | auto | method: String? = auto |
| `verify` | Boolean | no | false | verify: Boolean = false |

Returns: `Unit`

<details><summary>Full documentation</summary>

tab.type(text: String)
tab.type(text: String, selector: String?)
tab.type(text: String, selector: String, method: String?, verify: Boolean?)

Types text into the currently focused element when selector is omitted.
When selector is provided, the executor focuses the matched element first and then types text.
method: auto|chars|exec — see the tool description. method/verify require a selector.

</details>

### `uncheck`, `browser_uncheck`

```
tab.uncheck(selector: String)
```

This method uncheck an element with [selector]. If there's no element matching [selector], nothing to do.

| Argument | Type | Required | Default | Meaning |
|---|---|---|---|---|
| `selector` | String | yes |  | The selector of the element, multiple formats supported. If there are multiple matching elements, the first will be unchecked. |

Returns: `Unit`

<details><summary>Full documentation</summary>

This method uncheck an element with [selector]. If there's no element matching [selector], nothing to do.

elements, the first will be unchecked.

</details>

### `upload`, `browser_file_upload`

```
tab.upload(selector: String, paths: List<String>)
```

Upload files to the element located by [selector].

| Argument | Type | Required | Default | Meaning |
|---|---|---|---|---|
| `selector` | String | yes |  | The selector of the element, multiple formats supported. It should resolve to the file input element. |
| `paths` | List<String> | yes |  | The list of file paths to upload. |

Returns: `Unit`

### `url`

```
tab.url()
```

The URL read-only property of the Document interface returns the document location as a string.

Returns: `String`

<details><summary>Full documentation</summary>

The URL read-only property of the Document interface returns the document location as a string.

This property equals to javascript `document.URL`. The `document.URL` property returns the same value. The `document.documentURI` property can be used on any document types, while the `document.URL` property can only be used on HTML documents.

</details>

### `wait_for_function`

```
tab.waitForFunction(pageFunction: String, timeout: Duration)
```

Returns when the pageFunction returns a truthy value.

| Argument | Type | Required | Default | Meaning |
|---|---|---|---|---|
| `pageFunction` | String | yes |  | A JavaScript function to be evaluated in the page context. |
| `timeout` | Duration | yes |  | timeout: Duration |

Returns: `WebDriver?`

### `wait_for_navigation`

```
tab.waitForNavigation(oldUrl: String, timeout: Duration)
```

Wait until the current url changes or timeout.

| Argument | Type | Required | Default | Meaning |
|---|---|---|---|---|
| `oldUrl` | String | yes |  | oldUrl: String |
| `timeout` | Duration | yes |  | The maximum time to wait for the url to change. |

Returns: `Duration`

Examples:

- Example usage:

  ```kotlin
  * val timeout = Duration.ofSeconds(30)
       * val url = "https://www.example.com"
       * driver.navigate(url)
       * var remainingTime = driver.waitForNavigation(timeout)
       * if (remainingTime > 0) {
       *   driver.click("a[href='/next']")
       *   remainingTime = driver.waitForNavigation(url, timeout)
       * }
       *
  ```

<details><summary>Full documentation</summary>

Wait until the current url changes or timeout.

```kotlin val timeout = Duration.ofSeconds(30) val url = "https://www.example.com" driver.navigate(url) var remainingTime = driver.waitForNavigation(timeout) if (remainingTime > 0) { driver.click("a[href='/next']") remainingTime = driver.waitForNavigation(url, timeout) } ```

</details>

### `wait_for_page`

```
tab.waitForPage(url: String, timeout: Duration)
```

Await navigation to the specified URL page or timeout if necessary.

| Argument | Type | Required | Default | Meaning |
|---|---|---|---|---|
| `url` | String | yes |  | The URL to navigate to. |
| `timeout` | Duration | yes |  | timeout: Duration |

Returns: `WebDriver?`

Examples:

- Example usage:

  ```kotlin
  * val newDriver = driver.waitForPage("https://www.example.com", Duration.ofSeconds(30))
       *
  ```

<details><summary>Full documentation</summary>

Await navigation to the specified URL page or timeout if necessary.

```kotlin val newDriver = driver.waitForPage("https://www.example.com", Duration.ofSeconds(30)) ```

TODO: check if waitForPage and waitForNavigation can be merged into one method.

</details>

### `wait_for_selector`

```
tab.waitForSelector(selector: String, timeout: Duration, action: suspend ()
```

Wait for the element identified by the selector to become present in the DOM, or until timeout.

| Argument | Type | Required | Default | Meaning |
|---|---|---|---|---|
| `selector` | String | yes |  | The selector of the element, multiple formats supported. The element is waited for until it becomes present. |
| `timeout` | Duration | yes |  | The maximum time to wait for the element to become present. |
| `action` | suspend ( | yes |  | The action to execute when the element is not found. |

Returns: `Unit`

Examples:

- Example usage:

  ```kotlin
  * val remainingTime = driver.waitForSelector("h2.title", Duration.ofSeconds(30)) {
       *  driver.scrollDown()
       * }
       *
  ```

<details><summary>Full documentation</summary>

Wait for the element identified by the selector to become present in the DOM, or until timeout.

This method periodically checks for the existence of the element. If the element is not found during a check, the action will be executed, such as scrolling the page down.

```kotlin val remainingTime = driver.waitForSelector("h2.title", Duration.ofSeconds(30)) { driver.scrollDown() } ```

</details>

## Domain `webdb`

### `webdb_export`

```
webdb.export(sessionId: String, urls: String, outputDir: String)
```

Export pages from the web database to a local directory. Provide a comma-separated list of URLs.

| Argument | Type | Required | Default | Meaning |
|---|---|---|---|---|
| `sessionId` | String | yes |  | Session whose web database is exported. Required. |
| `urls` | String | yes |  | Comma-separated URL list to export; omit to export everything the session stored. |
| `outputDir` | String | yes |  | Target directory; omit for the server default. |

Returns: `String`

Examples:

- Export two pages: `{"sessionId": "<session-id>", "urls": "https://example.com,https://example.org"}`

### `webdb_normalize`

```
webdb.normalize(sessionId: String, url: String)
```

Normalize a URL for use as a web database key. Resolves redirects, normalizes paths, and validates the URL.

| Argument | Type | Required | Default | Meaning |
|---|---|---|---|---|
| `sessionId` | String | yes |  | Session used to resolve redirects. Required. |
| `url` | String | yes |  | URL to normalize. Required. |

Returns: `String`

Examples:

- Normalize a URL with query parameters: `{"sessionId": "<session-id>", "url": "https://example.com/a?b=1"}`
  - Returns the canonical key, e.g. https://example.com/a?b=1

