package ai.platon.pulsar.pdk.testplugin.integration

/**
 * Browse-phase event handler for the PDK test plugin.
 *
 * Demonstrates the handler separation pattern where event logic lives in
 * dedicated classes rather than directly in the auto-configuration.
 */
open class TestBrowseEventHandler {
    // Add test handler logic here
}
