# Arbitrary CDP commands support

Add an interface to WebDriver to send arbitrary CDP commands to the browser. This allows for more advanced interactions
with the browser that are not covered by the standard WebDriver API. The interface should support sending commands and
receiving responses in a structured format, such as JSON.

#auto-approve
