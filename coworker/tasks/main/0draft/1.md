# implement two new CLI

```
browser4-cli snapshot --depth=4          # limit tree depth
browser4-cli snapshot --raw
```

Raw output
Use --raw to strip page info and return only command output:

```
browser4-cli snapshot --raw | grep "button"
```

Remember update the documents.
