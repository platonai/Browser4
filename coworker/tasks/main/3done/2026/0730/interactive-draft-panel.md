Title: support `b4w coworker draft --interactive`
Description: Task drafted via coworker CLI.
Prompt: create drafts interactively. create a ps based minimal panel to list the existing drafts, create a new issue drafts, call `b4w.ps1 coworker fix` to fix a created issue optionally.

support short form of the flag: -i.
