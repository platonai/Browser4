## Daily Memory - 2026-03-13

- **Fixed Browser4 MCP launch guidance for packaged jars:** Investigated a failed `java -cp ... Browser4.jar ai.platon.pulsar.app.Browser4Launcher --app=mcp` startup and confirmed `target\Browser4.jar` is a Spring Boot fat jar with app classes under `BOOT-INF\classes`. Updated MCP runner KDoc and `browser4\browser4-agents\README.md` to use `java -jar ... --app=mcp` and to warn that direct `-cp` launch of `Browser4Launcher` fails for the packaged jar. **Key learning:** For Browser4 executable jars used by MCP clients, use `-jar`; direct classpath launch is only valid for plain jars.

- **Validated with module-local builds/tests because the root reactor is incomplete:** Root Maven validation failed due to a missing `browser4\browser4-spa` child module, so checks were rerun with module-local `-f` commands. `browser4\browser4-agents` compiled, `Browser4LauncherTest` passed, `browser4-agentic` compiled, and `java -jar ... --app=mcp` smoke-launched without the previous class-not-found error. **Key learning:** In the current repo state, prefer module-local Maven commands for targeted validation when the root reactor is broken.

- **Stabilized snapshot service E2E assertions around current aria output:** Reproduced `SnapshotServiceE2ETest` failures in `browser4-tests\pulsar-it-tests` and confirmed the renderer now produces a compact but stable aria snapshot such as `RootWebArea "Dynamic Content Test" [ref=e2]`, not the older long fixture dump. Updated `SnapshotServiceE2ETest` to wait for the heading, seed the virtual list deterministically, reuse `collectEnhancedRoot(...)`, and assert semantic snapshot content instead of brittle length thresholds. Aligned `SnapshotServiceFullCoverageTest` to the same heading wait and semantic expectation. **Key learning:** For Browser4 snapshot E2E coverage, assert stable accessibility semantics (for example `RootWebArea` plus page title) rather than raw snapshot size, since compact output can still represent a correct selector-map/tree pipeline.

- **Restored anchor navigation attributes in nano DOM serialization:** Investigated `DOMStateBuilderTest` failures showing anchor nodes lost `target` after `DOMStateBuilder.build(...).microTree.toNanoTreeUnfiltered()`. Added navigation/media attributes (`src`, `action`, `target`, `rel`, `download`) to `DefaultIncludeAttributes.ATTRIBUTES`, re-enabled `testHrefAndNavigationAttributesArePreservedInNanoDOMTree`, and confirmed the module-local `pulsar-browser` test class passes. **Key learning:** `MORE_ATTRIBUTES` is not included by default in `DOMStateBuilder.build()`, so any attribute required by the default nano-tree contract must also be present in `DefaultIncludeAttributes.ATTRIBUTES`.


- **Handled an empty coworker task file without making repository changes:** Inspected `coworker\tasks\2working\task-from-1-md.md` and confirmed it is a zero-byte file, so there were no actionable implementation instructions to execute. Recorded the outcome instead of inventing work, preserving the repository state. **Key learning:** For coworker queue processing, verify the task file content first; if a task artifact is empty, treat it as a no-op and document that fact rather than making speculative changes.

- **Verified monthly memory was already up to date for pre-today summaries:** Checked `coworker\tasks\300logs\2026\03\MEMORY.202603.md` and confirmed it already contains the prior-day rollup status through `2026-03-12`, so no monthly append was needed. **Key learning:** Memory maintenance should be append-only and conditional; only add a monthly rollup when the previous day's coverage is actually missing.
