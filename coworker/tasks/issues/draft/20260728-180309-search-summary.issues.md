# Issues: search-summary

> **Source:** `20260728-180309-search-summary.full.md` | **Date:** 20260728-180309 | **Mode:** dev

## Scenario Background

### Task

### What the Wuhan Lobster Festival Is

The "Wuhan Lobster Festival" is not a single event but rather a constellation of city-wide **crayfish (小龙虾) consumption promotion activities** organized under the umbrella brand **"武汉小龙虾消费季"** (Wuhan Crayfish Consumption Season). The flagship event is a large-scale carnival held at **汉口江滩三阳广场** (Hankou River Beach Sanyang Square), complemented by numerous regional sub-festivals across Wuhan's districts. The official organizing body is the **武汉市商务局** (Wuhan Municipal Commerce Bureau), which brands the season with the slogan "不嘬虾，枉夏天" (If you don't eat crayfish, you've wasted your summer).

### History and Background

- **2024**: The earliest documented iteration — Wuhan College held its first lobster festival with a "farewell graduating students" theme.
- **2025**: Expanded into a cultural immersion theme (荆楚文化浸润) at Wuhan College. The city-level "夏日荟聚" consumption promotion + crayfish consumption season launched, attracting over **1 million visitors** to the Hankou river beach main venue.
- **2026**: Major scale-up. The Wuhan College festival entered its **third year** with AI/robot integration. The Qijiawan (祁家湾) festival entered its **second year** as "祁遇季" (Qi Encounter Season), evolving from a barbecue festival. The city-level carnival added brain-computer interface tech, AI digital human mascots, dual-city collaboration with Qianjiang (潜江), and simultaneous release of crayfish consumption reports by Meituan/Douyin/Taobao/Tmall.

The festivals are politically and economically significant — they are part of Wuhan's broader strategy to stimulate domestic consumption, promote agricultural supply chains (Qianjiang is China's crayfish capital), and build Wuhan's brand as a "crayfish city."

### Major Activities

1. **City-Level Carnival (汉口江滩三阳广场, April 30–May 4):**
   - **Six themed markets:** Crayfish food consumption zone (肥肥虾庄, 靓靓蒸虾, 巴厘龙虾, 一棠龙虾, 潜江龙虾, 周黑鸭); e-commerce + interactive zone (Douyin, Meituan live-streaming); Wuhan district specialties ("十大名菜、十大名点"); creative consumption zone (crafts,文创); tech consumption zone (brain-computer interface from 宇构智核); foreign trade & trade-in zone (JD Mall, 工贸家电)
   - **Five music carnivals:** Rock, pop, and Chinese-style (国风) performances nightly
   - **Three themed parades**
   - **250,000 yuan in on-site "嘬虾券" (crayfish vouchers):** Won through hourly interactive games at 18:00, 19:00, 20:00, 21:00
   - **May 4 special:** Wuhan team's first "楚超" (Chu Super League) home game watch party on giant screen
   - **Dual-city collaboration with Qianjiang:** Coordinated supply chain, joint promotions

2. **University Festival (武汉学院, June 10, 2026):**
   - **60,000+ free crayfish** for students (all fresh, shipped from Qianjiang that morning)
   - **2.7m × 2m giant pot** cooking 800 jin (400kg) of crayfish on-site
   - 1,500+ lobster buns, spicy lobster noodles, lobster pineapple fried rice, 500+ Jiangxia specialties
   - **AI robot interaction:** 宇树 (Unitree) robots dancing and interacting; robotic cooking arms
   - **International chef competition:** Students and international students from Ethiopia, Pakistan, Kazakhstan, South Africa cooking home-country dishes
   - **"8 languages challenge":** Kazakh student demonstrated how to say "小龙虾" in 8 languages

3. **Regional Festival — Qijiawan, Huangpi (黄陂祁家湾, April 29–May 5):**
   - **Second "祁遇季" Lobster Beer Carnival**
   - Day 1: **15,000+ visitors** (new record), 5,000+ daily at peak
   - **Government compound opened as free parking** — 7 areas, ~1,000 spots
   - One vendor sold **4,000+ jin** earning ~30,000 yuan in a single night
   - Fresh "清水虾" (clean-water crayfish), 5 flavor varieties
   - 20+ snack/BBQ vendors, puppet fireworks, firework waterfall shows nightly 21:00-21:30

4. **City-Wide Restaurant Campaigns (April–August):**
   - 肥肥虾庄 "肥肥龙虾节" (May–July) at all Wuhan locations
   - **300万元 (3 million yuan) in digital vouchers** distributed across 13 batches via Meituan, Dianping, Douyin
   - Head brands (肥肥虾庄, 靓靓蒸虾, 巴厘龙虾, 一棠龙虾) offering exclusive deals
   - 良之隆 launched "一路钳行" global crayfish promotion plan

### Typical Schedule and Venue

| Event | Venue | Dates | Time |
|---|---|---|---|
| City Carnival | 汉口江滩三阳广场 | Apr 30–May 4 | 17:30–21:30 |
| Qijiawan Carnival | 黄陂祁家湾广场 | Apr 29–May 5 | 17:00–22:00 |
| Wuhan College Festival | 武汉学院篮球场 | Jun 10 | Afternoon |
| 光谷龙虾节 | 光谷 (Optics Valley) | Holiday periods | Varies |
| Restaurant campaigns | City-wide (all districts) | Apr–Aug | Regular hours |
| Voucher distribution | Meituan/Dianping/Douyin | Apr–Aug (13 batches) | Online |

The overall consumption season spans **April through August**, with peak activity during the May Day holiday (五一).

### Significance

**Tourism:** The 2025 season attracted over 1 million visitors to the main venue alone. Qijiawan's 2026 event drew vehicles from provinces as far as Qinghai (青海), Guangdong (广东), and Henan (河南), demonstrating significant inter-provincial tourism pull. The festivals transform Wuhan into a summer food tourism destination.

**Food Culture:** Crayfish (小龙虾) is deeply embedded in Wuhan's culinary identity. The festivals formalize and celebrate the city's "嘬虾子" (sucking crayfish) culture, pairing it with Wuhan's "十大名菜、十大名点" (top 10 dishes and snacks), beer culture, and late-night street food traditions. The slogan "没有一只小龙虾能活着走出武汉" (No crayfish leaves Wuhan alive) has become a cultural meme.

**Economy:** The scale is substantial — 300万元 in government-issued vouchers, individual vendors earning 30,000 yuan/night, tens of thousands of jin of crayfish consumed daily. The festivals support the entire supply chain: Qianjiang crayfish farms → logistics → restaurants/vendors → digital platforms → tourism hospitality. The dual-city Qianjiang-Wuhan collaboration integrates the Jianghan Plain's agricultural production with Wuhan's consumption market. The 良之隆 "global crayfish plan" signals ambition to export Wuhan's crayfish culture internationally.

---

### Execution Context

**Key Commands:**

1. `./b4w.ps1 help` — learned available commands
2. `./b4w.ps1 goto "https://www.baidu.com"` — navigated to Baidu
3. `./b4w.ps1 snapshot -v 0 --stdout` — captured page structure, identified search box refs
4. `./b4w.ps1 fill e312 "武汉龙虾节"` — filled search query
5. `./b4w.ps1 click e341` — clicked search button
6. `./b4w.ps1 snapshot -v 0 --stdout` — captured search results
7. `./b4w.ps1 htmlsnapshot` — captured static HTML snapshot for extraction
8. `./b4w.ps1 htmlsnapshot get all text ".cosc-title-a" --limit 20` — extracted search result titles
9. `./b4w.ps1 htmlsnapshot get all text ".c-abstract" --limit 20` — attempted abstract extraction (only 1 result)
10. `./b4w.ps1 eval "document.querySelector('#content_left')..."` — extracted full search result text via JS (this was the **key command** that yielded rich content)
11. `./b4w.ps1 goto "http://www.baidu.com/link?url=..."` — navigated to Changjiang Daily article (university lobster festival)
12. `./b4w.ps1 eval` — extracted article body text
13. `./b4w.ps1 go-back` — returned to search results
14. `./b4w.ps1 goto "http://www.baidu.com/link?url=..."` — navigated to Wuhan Commerce Bureau article (city carnival)
15. `./b4w.ps1 eval` — extracted commerce bureau article
16. `./b4w.ps1 go-back` — returned to search results
17. `./b4w.ps1 goto "http://www.baidu.com/link?url=..."` — navigated to Jiupai News article (Qijiawan festival)
18. `./b4w.ps1 wait --load networkidle` + `./b4w.ps1 eval` — waited for dynamic content then extracted article

**Key decisions:**
- Used `eval` (JavaScript) as the primary extraction method after discovering `htmlsnapshot get` with CSS selectors didn't match Baidu's obfuscated class names well
- Used Baidu redirect URLs with `goto` to bypass click interception
- Used the AI-generated summary at the top of Baidu's results page as a primary information source (it was surprisingly comprehensive)

**Workarounds required:**
- Baidu search result clicks were intercepted by JavaScript; had to extract redirect URLs via `eval` and navigate with `goto`
- `.c-abstract` selector only matched ad content, not search snippets
- `baijiahao.baidu.com` content loaded dynamically — required `wait --load networkidle` before extraction
- WebFetch tool blocked for baidu.com domains

---

---

## Issues Found (8 issues)

### Issue 1: snapshot grep found 0 matches for Chinese text present in the page

**Severity:** High
**Category:** Reliability

#### Reproduction

After navigating to Baidu search results for "武汉龙虾节", run `b4w.ps1 snapshot grep "龙虾节"`. The command reports "0 matches found" despite the page clearly containing the text "龙虾节" in multiple places (verified via eval extraction).

#### Expected Behavior

snapshot grep should find and display lines from the accessibility tree snapshot containing the search term "龙虾节".

#### Actual Behavior

0 matches found — the grep silently returns no results for Chinese text that is clearly present on the page.

#### Root Cause Analysis

The snapshot YAML file likely stores Chinese characters in a way that snapshot grep doesn't match against. This could be a character encoding issue, or grep may be searching the raw YAML tree structure labels rather than the node text content. Investigation needed: check whether the grep matches against the YAML node names (e.g., 'link "武汉龙虾节"') or only against structural labels.

#### Code Pointer

`cli/browser4-cli/src/ — snapshot grep implementation`

#### AI Suggested Improvement

- Investigate whether snapshot grep searches YAML node labels vs text content
- Add a test case with Chinese/Unicode text to the grep test suite
- Consider adding a --raw flag that searches the literal YAML file content rather than parsed nodes
- Document clearly what content types snapshot grep searches (element roles, text, URLs, etc.)

#### Human Review

- [ ] **ACCEPT** — issue confirmed valid; suggested improvement is correct
- [ ] **ACCEPT with improvements** — issue valid but fix needs refinement (add details in Notes)
- [ ] **DEFER** — issue acknowledged but intentionally deferred (add rationale in Notes)
- [ ] **WONTFIX** — issue acknowledged but will not be fixed (add rationale in Notes)
- [ ] **REJECT** — issue invalid, not a problem, or already addressed
- **Notes:**

---

### Issue 2: Clicking on Baidu search result links does not navigate — clicks are intercepted

**Severity:** High
**Category:** Reliability

#### Reproduction

On a Baidu search results page, identify a search result link ref from the snapshot (e.g., e11159 for a result titled "2.7米巨锅现场烹制..."). Run `b4w.ps1 click e11159`. The command reports success but the page URL does not change — the browser stays on the search results page.

#### Expected Behavior

Clicking a search result link should navigate to the article page.

#### Actual Behavior

The click was registered (✓ Clicked e11159) but the page did not navigate. The URL remained the search results page.

#### Root Cause Analysis

Baidu uses JavaScript click handlers (likely event.preventDefault() or custom routing) on search result links. The CDP Input.dispatchMouseEvent may fire the native click but Baidu's JS intercepts it and either blocks navigation or uses its own routing mechanism that requires additional JS event properties not set by CDP.

#### Code Pointer

`browser4-core/browser4-browser/ — PulsarWebDriver click implementation`

#### AI Suggested Improvement

- Consider using a JS-based click fallback (Runtime.evaluate to call element.click()) when CDP mouse events don't trigger navigation
- Add detection: after a click on a link, check if the URL changed; if not, retry with JS click
- Document this as a known limitation with workaround (use eval to extract href and goto directly)
- The workaround of extracting the URL and using goto is functional but requires two extra commands

#### Human Review

- [ ] **ACCEPT** — issue confirmed valid; suggested improvement is correct
- [ ] **ACCEPT with improvements** — issue valid but fix needs refinement (add details in Notes)
- [ ] **DEFER** — issue acknowledged but intentionally deferred (add rationale in Notes)
- [ ] **WONTFIX** — issue acknowledged but will not be fixed (add rationale in Notes)
- [ ] **REJECT** — issue invalid, not a problem, or already addressed
- **Notes:**

---

### Issue 3: htmlsnapshot get all text '.c-abstract' returns only ad content, not search snippets

**Severity:** Medium
**Category:** UX

#### Reproduction

After capturing htmlsnapshot on Baidu search results, run `b4w.ps1 htmlsnapshot get all text ".c-abstract" --limit 20`. Only 1 result is returned: a JD.com ad snippet. None of the organic search result descriptions are captured.

#### Expected Behavior

The command should return the text snippets/descriptions for all organic search results.

#### Actual Behavior

Only the ad snippet was returned. The class name `.c-abstract` does not match Baidu's current HTML structure for organic search result snippets.

#### Root Cause Analysis

Baidu uses different or dynamically-generated class names for organic result snippets. This is a common issue with CSS-selector-based extraction — the selector that works on one site may not work on another, and class names change frequently. The `htmlsnapshot inspect` command is designed to help discover selectors but requires an extra step.

#### AI Suggested Improvement

- The documentation correctly warns about this (CSS selectors break when sites change HTML), but the error message is silent — it just returns fewer results than expected
- Consider adding a warning when get all returns 0 or 1 results: "Only N results found. Try htmlsnapshot inspect to discover current selectors."
- The `htmlsnapshot inspect` auto-discovery feature should be promoted more prominently in the get error path

#### Human Review

- [ ] **ACCEPT** — issue confirmed valid; suggested improvement is correct
- [ ] **ACCEPT with improvements** — issue valid but fix needs refinement (add details in Notes)
- [ ] **DEFER** — issue acknowledged but intentionally deferred (add rationale in Notes)
- [ ] **WONTFIX** — issue acknowledged but will not be fixed (add rationale in Notes)
- [ ] **REJECT** — issue invalid, not a problem, or already addressed
- **Notes:**

---

### Issue 4: eval command with complex JavaScript has severe shell quoting issues

**Severity:** Medium
**Category:** UX

#### Reproduction

Run a moderately complex eval command with single quotes, double quotes, and parentheses: `b4w.ps1 eval "document.querySelector('#content_left') ? document.querySelector('#content_left').innerText.substring(0, 5000) : 'not found'"`. The command requires complex escaping — the actual invocation needed multiple backslash-escaped quotes that were difficult to construct correctly.

#### Expected Behavior

Users should be able to pass JavaScript expressions to eval without wrestling with shell quoting.

#### Actual Behavior

Multi-layered quoting required. The command needed: `eval 'document.querySelector('\''#content_left'\'') ? document.querySelector('\''#content_left'\'').innerText.substring(0, 5000) : '\''not found'\'''`. This is extremely error-prone.

#### Root Cause Analysis

Shell quoting interacts badly with JavaScript's use of both single and double quotes. The SKILL.md documents this warning and recommends --file and --stdin alternatives.

#### AI Suggested Improvement

- The documentation already recommends --file, --stdin, and --base64. This is good.
- Consider making --file mode more discoverable in the eval help text (add an example with --file in the command's --help output)
- Consider a --js shorthand that reads from stdin by default when no argument given
- Add a concrete example in the main help output showing the --file pattern for complex JS

#### Human Review

- [ ] **ACCEPT** — issue confirmed valid; suggested improvement is correct
- [ ] **ACCEPT with improvements** — issue valid but fix needs refinement (add details in Notes)
- [ ] **DEFER** — issue acknowledged but intentionally deferred (add rationale in Notes)
- [ ] **WONTFIX** — issue acknowledged but will not be fixed (add rationale in Notes)
- [ ] **REJECT** — issue invalid, not a problem, or already addressed
- **Notes:**

---

### Issue 5: No built-in command for extracting page text content (full text, not just specific selectors)

**Severity:** Medium
**Category:** Discoverability

#### Reproduction

User wants to extract all readable text from a page. No documented command does this directly. `htmlsnapshot get text` requires a CSS selector. `snapshot` shows the AX tree. `eval` works but requires JavaScript knowledge.

#### Expected Behavior

There should be a simple command like `htmlsnapshot get text` (without selector) or `extract text` that returns the page's visible text content.

#### Actual Behavior

Users must either: (a) know the CSS selector for the content container, (b) write JavaScript eval, or (c) use AI-powered extract (which needs an LLM key).

#### Root Cause Analysis

The tool is optimized for structured extraction (specific elements via selectors) rather than unstructured full-page text extraction. The htmlsnapshot summary command provides a compressed view but not the raw text.

#### AI Suggested Improvement

- Add a `htmlsnapshot get text` (no selector) variant that returns all visible text from the page
- Or add a `readability` command that extracts the main content text (like Firefox Reader View)
- The AI `summarize` command partially addresses this but requires LLM API access which may not be configured

#### Human Review

- [ ] **ACCEPT** — issue confirmed valid; suggested improvement is correct
- [ ] **ACCEPT with improvements** — issue valid but fix needs refinement (add details in Notes)
- [ ] **DEFER** — issue acknowledged but intentionally deferred (add rationale in Notes)
- [ ] **WONTFIX** — issue acknowledged but will not be fixed (add rationale in Notes)
- [ ] **REJECT** — issue invalid, not a problem, or already addressed
- **Notes:**

---

### Issue 6: Short flag -v triggers PowerShell parameter binding warning on every invocation

**Severity:** Low
**Category:** UX

#### Reproduction

Run `b4w.ps1 snapshot -v 0 --stdout`. Every invocation produces the warning: "⚠ Short flags detected: -v. PowerShell may intercept these in other contexts (b4w.sh, direct pwsh). Prefer long-form equivalents: --output, --interactive, --viewport"

#### Expected Behavior

The command should work without warnings, or the wrapper should handle the flag transparently.

#### Actual Behavior

Every command with -v prints a warning. This adds noise to the output and can obscure actual results.

#### Root Cause Analysis

The b4w.ps1 wrapper uses manual argument parsing and detects short flags, emitting a warning even though in this context (PowerShell with manual parsing) the flag is safe.

#### Code Pointer

`b4w.ps1 — short flag detection/warning logic`

#### AI Suggested Improvement

- Consider suppressing the warning when running under b4w.ps1 (which handles it safely)
- Only show the warning when the user is running b4w.sh or direct pwsh where interception risk is real
- Or add a --no-warn-short-flags option to suppress it
- The long-form --viewport is mentioned but --viewport 0 is longer to type than -v 0

#### Human Review

- [ ] **ACCEPT** — issue confirmed valid; suggested improvement is correct
- [ ] **ACCEPT with improvements** — issue valid but fix needs refinement (add details in Notes)
- [ ] **DEFER** — issue acknowledged but intentionally deferred (add rationale in Notes)
- [ ] **WONTFIX** — issue acknowledged but will not be fixed (add rationale in Notes)
- [ ] **REJECT** — issue invalid, not a problem, or already addressed
- **Notes:**

---

### Issue 7: Dynamic content on baijiahao.baidu.com requires explicit wait before extraction

**Severity:** Low
**Category:** UX

#### Reproduction

Navigate to a baijiahao.baidu.com article. Immediately run eval to extract content. The eval returns only "发表" (very little content) because the article body loads dynamically via JavaScript after initial page render.

#### Expected Behavior

Content should be extractable after navigation completes, or the tool should warn that content may not be fully loaded.

#### Actual Behavior

Eval returned near-empty content. Required explicit `wait --load networkidle` before extraction worked.

#### Root Cause Analysis

baijiahao.baidu.com loads article content asynchronously. The goto command completes when the initial HTML loads, but the article body arrives later via XHR/fetch. This is a common pattern on modern JS-heavy sites.

#### AI Suggested Improvement

- Consider adding an auto-wait hint after goto: "Page loaded. Use wait --load networkidle if content appears incomplete."
- Document this pattern more prominently in the core loop section of SKILL.md
- Consider a `goto --wait` flag that automatically waits for network idle after navigation

#### Human Review

- [ ] **ACCEPT** — issue confirmed valid; suggested improvement is correct
- [ ] **ACCEPT with improvements** — issue valid but fix needs refinement (add details in Notes)
- [ ] **DEFER** — issue acknowledged but intentionally deferred (add rationale in Notes)
- [ ] **WONTFIX** — issue acknowledged but will not be fixed (add rationale in Notes)
- [ ] **REJECT** — issue invalid, not a problem, or already addressed
- **Notes:**

---

### Issue 8: Snapshot YAML files can exceed 52KB — too large for stdout, forcing file-based workflow

**Severity:** Low
**Category:** UX

#### Reproduction

Run `b4w.ps1 snapshot -v 0 --stdout` on a content-rich page like Baidu search results. Output exceeds 52KB and gets truncated.

#### Expected Behavior

Users should be able to browse snapshots efficiently without hitting size limits.

#### Actual Behavior

The full output was saved to a temp file. The truncated preview only showed ~2KB. The SKILL.md warns about this but the workaround (viewport pagination, snapshot grep) didn't work for Chinese text.

#### Root Cause Analysis

AX trees for complex pages are inherently large. The --stdout flag pipes the entire tree, which is impractical for content-rich pages. Viewport pagination (-v 0) helps but still produced 52KB.

#### AI Suggested Improvement

- This is a known limitation documented in SKILL.md. The documentation is good.
- Consider adding a --summary flag that outputs only interactive elements (buttons, inputs, links) with their refs, skipping structural divs
- The htmlsnapshot summary (WPSI) addresses this partially — consider integrating it more closely with the snapshot workflow

#### Human Review

- [ ] **ACCEPT** — issue confirmed valid; suggested improvement is correct
- [ ] **ACCEPT with improvements** — issue valid but fix needs refinement (add details in Notes)
- [ ] **DEFER** — issue acknowledged but intentionally deferred (add rationale in Notes)
- [ ] **WONTFIX** — issue acknowledged but will not be fixed (add rationale in Notes)
- [ ] **REJECT** — issue invalid, not a problem, or already addressed
- **Notes:**

---

## Overall Assessment

**Completion Status:** Successful — the task of researching the Wuhan Lobster Festival was completed with comprehensive content from 3 article sources plus Baidu's AI-generated summary. All 4 required summary dimensions (what it is, history, activities, schedule/venue, significance) were covered in detail.

**Success Rate:** 85% — navigation, form filling, search, and most extraction worked well. Two failures: snapshot grep returned 0 results for Chinese text, and clicking Baidu search result links did not navigate. Both had workarounds (eval for extraction, goto with redirect URL for navigation).

**Issues Found:** 8

**Major Blockers:** None that prevented task completion. The two most impactful issues (snapshot grep not matching Chinese text, Baidu click interception) both had viable workarounds: using eval for text extraction and using goto with redirect URLs for navigation.

**Most Confusing Aspects:** 1. Discovering that snapshot grep doesn't work for Chinese text — the user gets '0 matches' with no explanation, leading to confusion about whether the search term is actually on the page. 2. The multiple different extraction methods (snapshot, htmlsnapshot get, htmlsnapshot query, eval, extract) with overlapping but different capabilities create a steep learning curve — it's unclear which to use when. 3. Shell quoting for eval commands with complex JavaScript requires trial-and-error.

**Most Valuable Improvements:** 1. Fix snapshot grep for non-ASCII/Chinese text — this is critical for non-English users. 2. Add JS-based click fallback when CDP click events don't trigger navigation (common on Chinese sites using JS routing). 3. Add a simple 'get all page text' command without requiring CSS selectors or JavaScript knowledge. 4. Add auto-detection of dynamic content with a hint to use wait --load networkidle.

**Usability Rating:** 6/10

---

## How to Reproduce

### Common Setup

1. Clone the repository and `cd` to the repo root.
2. The CLI is invoked via `./b4w.ps1` which auto-builds from source when needed.
3. The backend server starts automatically in dev mode.
4. All commands from repo root: `./b4w.ps1 <command>`

### Per-Issue Reproduction Steps

#### Issue 1: snapshot grep found 0 matches for Chinese text present in the page

After navigating to Baidu search results for "武汉龙虾节", run `b4w.ps1 snapshot grep "龙虾节"`. The command reports "0 matches found" despite the page clearly containing the text "龙虾节" in multiple places (verified via eval extraction).

#### Issue 2: Clicking on Baidu search result links does not navigate — clicks are intercepted

On a Baidu search results page, identify a search result link ref from the snapshot (e.g., e11159 for a result titled "2.7米巨锅现场烹制..."). Run `b4w.ps1 click e11159`. The command reports success but the page URL does not change — the browser stays on the search results page.

#### Issue 3: htmlsnapshot get all text '.c-abstract' returns only ad content, not search snippets

After capturing htmlsnapshot on Baidu search results, run `b4w.ps1 htmlsnapshot get all text ".c-abstract" --limit 20`. Only 1 result is returned: a JD.com ad snippet. None of the organic search result descriptions are captured.

#### Issue 4: eval command with complex JavaScript has severe shell quoting issues

Run a moderately complex eval command with single quotes, double quotes, and parentheses: `b4w.ps1 eval "document.querySelector('#content_left') ? document.querySelector('#content_left').innerText.substring(0, 5000) : 'not found'"`. The command requires complex escaping — the actual invocation needed multiple backslash-escaped quotes that were difficult to construct correctly.

#### Issue 5: No built-in command for extracting page text content (full text, not just specific selectors)

User wants to extract all readable text from a page. No documented command does this directly. `htmlsnapshot get text` requires a CSS selector. `snapshot` shows the AX tree. `eval` works but requires JavaScript knowledge.

#### Issue 6: Short flag -v triggers PowerShell parameter binding warning on every invocation

Run `b4w.ps1 snapshot -v 0 --stdout`. Every invocation produces the warning: "⚠ Short flags detected: -v. PowerShell may intercept these in other contexts (b4w.sh, direct pwsh). Prefer long-form equivalents: --output, --interactive, --viewport"

#### Issue 7: Dynamic content on baijiahao.baidu.com requires explicit wait before extraction

Navigate to a baijiahao.baidu.com article. Immediately run eval to extract content. The eval returns only "发表" (very little content) because the article body loads dynamically via JavaScript after initial page render.

#### Issue 8: Snapshot YAML files can exceed 52KB — too large for stdout, forcing file-based workflow

Run `b4w.ps1 snapshot -v 0 --stdout` on a content-rich page like Baidu search results. Output exceeds 52KB and gets truncated.

