All evaluation work is complete. Here is the full deliverable.

# A. Task Result

**Task completed successfully.** The full form-filling workflow was executed against `http://localhost:18080/generated/form-filling.html` using only documented browser4-cli commands:

1. **Navigation:** `goto` loaded the page (reconnected to the pre-existing DEFAULT session — see issues).
2. **Discovery:** `snapshot -i --stdout` revealed all form controls: First Name `e1089`, Last Name `e1090`, Email `e1091`, Country dropdown `e1092`, Experience level dropdown `e1095`, Comments textarea `e1096`, topic checkboxes `e1100`–`e1102`, agree-terms `e1104`, Submit `e1255`.
3. **Text inputs filled** (via refs, then re-done via CSS selectors after a diagnostic reload): First Name = Alex, Last Name = Morgan, Email = alex.morgan@example.com, Comments = "Automating data extraction workflows across product catalogs."
4. **Dropdowns:** Country → Germany (value `de`), Experience level → Advanced.
5. **Checkboxes:** Testing, AI agents, and agree-terms checked (Automation was pre-checked — 4 total).
6. **Submit:** `click "#submit-btn"` → "✓ Clicked"; `wait --load networkidle` → "✓ Wait complete".
7. **Verification:** `get text e1972` returned the full submitted JSON payload (firstName Alex, lastName Morgan, email, country de, topics [automation, testing, ai], experienceLevel advanced); snapshot grep confirmed "Your form data was captured successfully." and "All validation checks passed."; live summary showed "5 / 5 required".
8. **Documentation snapshot:** `snapshot -v 0 --stdout` of the result page saved to `.test-sessions/form-result-final-viewport0.txt` (shows the success message, full payload, and all checked states).

# B. Execution Trace

**Commands used (in order):** `pwd` / mkdir `.test-sessions` → `./b4w.ps1 help` → read `skills/browser4-cli/SKILL.md` → curl check of MockSite (HTTP 200, already running — `./bin/test.ps1 mock-site` not needed) → `goto <url>` → `snapshot -i --stdout` → `fill` ×4 → `select --verify` ×2 → `check` ×3 → `snapshot grep "Submit form"` → `click e1255` → `wait --load networkidle` → `snapshot grep` ×2 → `get text e1972` → (`get`/`htmlsnapshot` diagnostics) → `reload` → `htmlsnapshot` retry → re-fill via CSS selectors (`fill "#first-name"`, etc.) → `select`/`check` via CSS → `click "#submit-btn"` → `wait` → `snapshot grep` → `get text e1972` → `scroll up 2000` → `snapshot -v 0 --stdout` (saved) → `-s fresh open --headless` + probes → `-s fresh close` → `status` / `list`.

**Important decisions:**
- Started from refs for interactions, switched to **CSS selectors** for the redo after a diagnostic `reload` wiped the client-side form state — CSS selectors proved stable across reloads and worked for `fill`, `select`, `check`, `click`.
- Verified with `get text <ref>` after `get attr`/CSS-selector forms of `get` failed (documented as issues).
- Left the pre-existing sessions untouched; created and closed my own `fresh` session for root-cause probes.

**Workarounds required:**
- **htmlsnapshot capture broken in the reused session** → verified via a brand-new named session instead; the task itself never needed htmlsnapshot because `get text <ref>` and `snapshot grep` covered verification.
- **Form redone once** after `reload` reset the page (client-side form state).
- **select --verify false-negative** → interpreted the ✓ line (correct) and ignored the misleading verification warning.

```json
{
  "issues": [
    {
      "title": "htmlsnapshot capture fails in reused/pre-existing sessions with cryptic error and no recovery path",
      "severity": "High",
      "category": "Reliability",
      "reproduction": "1. Have a browser4 session created by an earlier process (e.g. a prior run's DEFAULT session, visible via `list`). 2. `./b4w.ps1 goto http://localhost:18080/generated/form-filling.html` (reconnects). 3. `./b4w.ps1 htmlsnapshot` -> ERROR: html_snapshot_capture failed: ReferenceError: __pulsar_utils__ is not defined at <anonymous>:1:1 ... help: Capture the current page as an HTML snapshot with metadata, interactive elements, and link groups. 4. `./b4w.ps1 reload` then `htmlsnapshot` again -> same failure. 5. `./b4w.ps1 -s fresh open --headless <url>` + `./b4w.ps1 -s fresh htmlsnapshot` -> works.",
      "expected": "htmlsnapshot capture should succeed in any session, or fail with an actionable message telling the user how to recover (e.g. re-inject the helper script or open a fresh session).",
      "actual": "Capture fails deterministically in the reused session (twice, including after reload). Error text is a raw JS exception concatenated with the tool's static description ('... help: Capture the current page as an HTML snapshot ...') and its function signature — no recovery guidance. Only a brand-new session avoids the failure.",
      "rootCause": "The capture tool evaluates page JS that depends on a `__pulsar_utils__` helper injected at browser-context creation (browser4-core/browser4-protocol/.../InteractiveBrowserEmulator.kt:666, 'For some type of pages, the script can not be injected'). Reused sessions whose browser context was created under different conditions never got the injection, and reload does not re-inject it. The error formatting appends the tool's static description as 'help' text (browser4-rest/.../mcp/controller/MCPToolController.kt:1210), which reads like a debug dump.",
      "codePointer": "browser4-core/browser4-protocol/src/main/kotlin/ai/platon/pulsar/protocol/browser/emulator/impl/InteractiveBrowserEmulator.kt:666 (injection check) and browser4-rest/src/main/kotlin/ai/platon/pulsar/rest/mcp/controller/MCPToolController.kt:1210 (error formatting)",
      "suggestion": "- Detect the missing `__pulsar_utils__` at capture time and re-inject the helper script into the current page instead of failing\n- When re-injection is impossible, return an actionable error: 'The helper script is not present in this browser session. Run `close` and re-open the session (or `open --fresh`) and retry.'\n- Stop appending the tool's static description/signature to runtime errors; reserve `help` text for usage errors"
    },
    {
      "title": "`get` command selector support is inconsistent across modes and contradicts its own help and SKILL.md",
      "severity": "High",
      "category": "Product",
      "reproduction": "Fresh session on the form page. `./b4w.ps1 get text \".primary-button\"` -> 'Submit form' (works). `./b4w.ps1 get text \".result-data\"` -> 'No elements matched'. `./b4w.ps1 get text \"#result-data\"` -> works. `./b4w.ps1 get attr \"#result-data\" class` -> 'No elements matched \"#result-data\"'. `./b4w.ps1 get attr \"#email\" class` -> 'No elements matched'. `./b4w.ps1 get property \"#email\" value` -> 'No elements matched'. `./b4w.ps1 get text e1972` (ref) -> works.",
      "expected": "A documented, predictable selector contract: either CSS selectors work for all modes (as the help examples imply), or the AXTree limitation is stated up front and enforced consistently.",
      "actual": "Support varies by mode and by element type: refs work for text; `.primary-button` works for text but `.result-data` does not; `#id` works for text but not for attr/property; attr/property failed for every tested selector form. `get --help` describes `<selector>` as 'CSS selector or element reference (e.g. e5, .price, #main)' with CSS examples, then appends a warning that the command operates on the AXTree, not CSS. SKILL.md line ~578 shows `get text \"#contactForm > button.primary\"` as a CSS example.",
      "rootCause": "CLI (cli/browser4-cli/src/commands.rs:1254 CommandDef) maps each mode to a different backend tool: text -> select_first_text_or_null, attr -> select_first_attribute_or_null, property -> select_first_property_value_or_null, html/styles -> browser_evaluate, box -> bounding_box. Resolution lives in the compiled pulsar-browser dependency: refs resolve via CDP DOM.resolveNode (stable), CSS via DOM.querySelector, but ref-based JS/attr reads convert the ref to a generated CSS selector from the a11y-derived DOM snapshot; for generic divs without id/class the generated selector degrades to bare `div`, so querySelector hits the first div on the page and the attr read returns null -> CLI prints 'No elements matched'. Different modes thus have different selector support and error behavior; the exact `.result-data` vs `.primary-button` divergence (both valid live-DOM class selectors) needs follow-up inside the dependency jar.",
      "codePointer": "cli/browser4-cli/src/commands.rs:1254 (mode->tool mapping); browser4-agentic/src/main/kotlin/ai/platon/pulsar/agentic/tools/builtin/BrowserTabToolExecutor.kt:1282/1294/1374 (dispatch); resolution itself in pulsar-browser-4.11.2.jar (DOMHandler.doResolveLocator / findCssSelectorFromLocator)",
      "suggestion": "- Make selector semantics uniform: if the AXTree is the data model, reject or translate CSS selectors consistently across modes and say so in help\n- Prefer resolving refs via backend node ID for ALL modes (text, attr, property, box), avoiding the lossy ref->CSS conversion for generic elements\n- Align the help text, SKILL.md examples, and the runtime error hint so all three describe one behavior"
    },
    {
      "title": "\"No elements matched\" message conflates selector-miss with null attribute value",
      "severity": "Medium",
      "category": "UX",
      "reproduction": "Fresh session: `./b4w.ps1 get attr \"#email\" class` -> 'null / No elements matched \"#email\".' The element #email exists and was matched; only the attribute read came back null.",
      "expected": "When the element matched but the attribute/property is absent or null, the output should say so (e.g. 'attribute not found' or 'value is null'), not claim the element was not found.",
      "actual": "The CLI treats any null/empty tool result as a selector-miss (main.rs:5317 empty_result check) and prints the same 'No elements matched' message plus a hint to use htmlsnapshot instead, misleading the user into fixing the wrong thing (the selector) when the selector was fine.",
      "rootCause": "cli/browser4-cli/src/main.rs:5313-5336 (handle_get): 'null' results are indistinguishable from selector-miss because the backend tool returns null both when resolution fails and when the resolved element's attribute/property value is null.",
      "codePointer": "cli/browser4-cli/src/main.rs:5296 handle_get (empty_result handling)",
      "suggestion": "- Differentiate the two outcomes (backend should return a distinguishable result for 'element found, attribute null')\n- Adjust the message per mode: for attr/property, print 'Element matched but attribute/property is empty or missing' instead of 'No elements matched'"
    },
    {
      "title": "select --verify reports a false-negative when the user passes the visible option text",
      "severity": "Medium",
      "category": "UX",
      "reproduction": "`./b4w.ps1 select \"#country\" \"Germany\" --verify` -> prints `[\"de\"]`, then '✓ Selected 'Germany' in #country', then 'Verification: expected 'Germany' to be selected, but current value is 'de'.' The selection actually succeeded (value 'de' is Germany). Passing the raw value (`select \"#country\" \"de\" --verify`) verifies cleanly.",
      "expected": "Selecting by visible text should verify successfully, or the verification message should report both the selected value and its label ('de (Germany)') without sounding like a failure.",
      "actual": "The confirmation says success, then the verification line says the selection does not match — two contradictory statements in one output. Users selecting by visible label (the natural way) are told their selection failed when it did not.",
      "rootCause": "cli/browser4-cli/src/main.rs:8669 verify_select_result evaluates `element.value` (raw option value, e.g. 'de') and compares it to the user-provided string ('Germany'). No normalization of label->value happens. Additionally, handle_select_command at main.rs:8604-8606 prints the backend's raw result text (the `[\"de\"]` array) before the confirmation line.",
      "codePointer": "cli/browser4-cli/src/main.rs:8669 verify_select_result and :8604-8606 handle_select_command",
      "suggestion": "- Compare against the selected option's text when the requested value does not equal element.value, e.g. `element.selectedOptions[0].textContent`\n- On mismatch, report both actual value and label instead of a bare failure sentence\n- Suppress or format the raw backend result array (`[\"de\"]`) that leaks before the confirmation line"
    },
    {
      "title": "Snapshot refs silently expire between consecutive get calls with no explanation in the error",
      "severity": "Medium",
      "category": "UX",
      "reproduction": "On the result page: `./b4w.ps1 get text e1265` returns the full payload (success). Immediately after: `./b4w.ps1 get attr e1265 class` -> 'null / No elements matched \"e1265\".' Same ref, same page, seconds apart.",
      "expected": "Either refs stay usable for consecutive commands, or the failure message explains that the ref expired and how to get a fresh one (re-snapshot).",
      "actual": "A command that looks identical in targeting (same ref, same session) fails right after a success with the generic 'No elements matched' message, which points the user at CSS-selector extraction instead of the real cause (stale ref).",
      "rootCause": "Refs are CDP backend node IDs; every interaction/command can remap them (auto-snapshots run after commands). The docs warn refs are 'ephemeral', but the error path does not detect the ref-looking input (`e` + digits) and does not mention staleness. Combined with the null-conflation issue, the user gets doubly misleading feedback.",
      "codePointer": "cli/browser4-cli/src/main.rs:5296 handle_get (error message path)",
      "suggestion": "- When the selector matches `e<digits>`, add a hint to the 'No elements matched' message: 'Refs expire after page changes — re-run `snapshot` to get fresh refs.'\n- Prefer resolving refs by backend node ID for attr/property modes too, so the ref works for consecutive get calls"
    },
    {
      "title": "snapshot -i output is far noisier than documented; generic names concatenate all descendant text; viewport header misleading",
      "severity": "Low",
      "category": "UX",
      "reproduction": "`./b4w.ps1 snapshot -i --stdout` on /generated/form-filling.html. SKILL.md shows a 12-line example; actual output was ~110 lines including generic nodes whose names are 300+ character concatenations of all descendant text (e.g. the identitySection generic), and elements from all 3 viewports while the header says 'processingViewport: 0'.",
      "expected": "Interactive mode should list interactive controls compactly (as the docs' 12-line example suggests), and the viewport header should match the content shown.",
      "actual": "Heavy text-concatenation noise dominates the output; form fields are still discoverable but the output is hard to scan. Elements beyond viewport 0 (y > 1080) appear in a snapshot whose header claims viewport 0 of 3.",
      "rootCause": "AX generic nodes inherit the concatenated names of descendants; `-i` filters roles but keeps generic wrappers that aggregate text. The `-i` path appears to emit the full-tree interactive set regardless of viewport, while the header still reports viewport metadata from the default viewport-0 mode.",
      "codePointer": "cli/browser4-cli/src/snapshot.rs (interactive-mode filtering and viewport header) — needs verification of exact function names",
      "suggestion": "- Truncate very long generic names (e.g. 60 chars with ellipsis) and/or suppress generics whose only content is descendant text when `-i` is active\n- Make the header reflect what `-i` actually emitted (full page) or restrict `-i` to the selected viewport"
    },
    {
      "title": "htmlsnapshot interactive-element list numbering starts at 2 (off-by-one)",
      "severity": "Low",
      "category": "UX",
      "reproduction": "`./b4w.ps1 htmlsnapshot` on the form page -> 'Interactive Elements / Buttons (2): 2. #registration-form button#submit-btn.primary-button ... 3. ... / Inputs (14): 4. ...' The first listed element is numbered 2.",
      "expected": "Sequential numbering starting at 1.",
      "actual": "Numbering begins at 2 (no item 1 anywhere in the visible output).",
      "rootCause": "Likely a 1-based enumeration started with an off-by-one counter (or item 1 consumed by a hidden element) in the htmlsnapshot metadata rendering.",
      "codePointer": "cli/browser4-cli/src/main.rs handle_html_snapshot (interactive elements rendering) — needs verification of exact function name",
      "suggestion": "- Start the visible numbering at 1"
    },
    {
      "title": "status output shows 'Installed bundle: v4.12.3' next to 'Server version: 4.13.4-SNAPSHOT' with no explanation",
      "severity": "Low",
      "category": "Documentation",
      "reproduction": "`./b4w.ps1 status` in dev mode -> 'Installed bundle: v4.12.3 (at 2026-08-05...)' and 'Server version: 4.13.4-SNAPSHOT'.",
      "expected": "Dev-mode status should make clear which artifact is actually running (the local 4.13.4-SNAPSHOT backend) so a first-time user does not think they are testing a stale installed bundle.",
      "actual": "Two versions are shown side by side with no note explaining that dev mode ignores the installed bundle and runs the locally built JAR.",
      "rootCause": "The status command reports both the installed runtime bundle metadata and the running server version without a dev-mode annotation.",
      "codePointer": "cli/browser4-cli/src/main.rs handle_status (status output assembly) — needs verification of exact function name",
      "suggestion": "- Add a line like 'Dev mode: running local build (installed bundle not used)' when the daemon was started from the source tree"
    }
  ],
  "assessment": {
    "completionStatus": "Successful — all 9 task steps completed: page loaded, interactive snapshot taken, 4 text inputs filled with realistic values, 2 dropdowns selected (Country=de/Germany, Experience level=advanced/Advanced), 3 checkboxes checked (+1 pre-checked Automation = 4 total, >= 2 required), form submitted via click on the submit button, networkidle wait, submission verified via `get text` returning the full payload and via snapshot grep showing 'Your form data was captured successfully.' and 'All validation checks passed.', and a full-viewport (-v 0) snapshot of the result page saved to .test-sessions/form-result-final-viewport0.txt. The form was completed twice: an intermediate `reload` (run to diagnose an htmlsnapshot failure) reset the client-side form state, so the fill+submit sequence was redone using CSS selectors, which worked flawlessly.",
    "successRate": "90% — the core interaction loop (goto, snapshot, fill, select, check, click, wait, snapshot grep, get text via ref, scroll, snapshot -v 0) worked first try; the extraction path (htmlsnapshot capture, get with CSS selectors/attr mode) failed in the reused session and required a fresh session workaround; select --verify gave misleading output but the underlying selection was correct.",
    "issuesFound": 8,
    "majorBlockers": "htmlsnapshot capture (and therefore all htmlsnapshot get/inspect/summary/export workflows) was completely broken in the pre-existing DEFAULT session due to missing __pulsar_utils__ injection; verified that a brand-new session works. No blocker prevented task completion — the `get text <ref>` path and snapshot grep covered verification.",
    "mostConfusingAspects": "1) `get` accepts some CSS selectors in some modes but not others, with help text, SKILL.md examples, and runtime errors all contradicting each other; 2) `select --verify` printed success and failure simultaneously when given the visible option text; 3) a ref that worked for `get text` failed seconds later for `get attr` with no staleness explanation; 4) htmlsnapshot's cryptic `__pulsar_utils__` error with no hint that a fresh session fixes it.",
    "mostValuableImprovements": "1) Re-inject or recover the helper script so htmlsnapshot works in every session, with an actionable error message otherwise; 2) unify `get` selector semantics across modes (resolve refs by backend node ID for attr/property too) and align help/docs with reality; 3) fix `select --verify` to compare labels against the selected option's text; 4) make 'No elements matched' distinguish selector-miss from null attribute and add a stale-ref hint.",
    "usabilityRating": 7
  }
}
```

---

**Notable positives (for context on the rating):** the core loop is genuinely pleasant — auto-snapshots after every interaction, `snapshot grep` as a fast verification tool, rotating tips, `✓` confirmations, and the helpful "No elements matched" hint pointing to the htmlsnapshot workflow. CSS-selector targeting for `fill`/`select`/`check`/`click` worked flawlessly and removed the ref-staleness worry for the interaction phase. The main deductions are the reliability gap in reused sessions (issue 1), the unpredictable `get` selector contract (issue 2), and several misleading-output problems that send users down the wrong debugging path.

Artifacts in `.test-sessions/`: `form-result-final-viewport0.txt` (result page snapshot), `form-filling-evidence-log.md` (full evidence log with source pointers), `form-filling-evaluation-20260814.json` (this report in machine-readable form).
