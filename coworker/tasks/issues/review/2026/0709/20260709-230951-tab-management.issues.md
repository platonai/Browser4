# Issues: tab-management

> **Source:** `20260709-230951-tab-management.full.md` | **Date:** 20260709-230951 | **Mode:** dev

### Execution Context

**Key Commands:**

```
 1. cargo run --manifest-path cli/browser4-cli/Cargo.toml -- --help
 2. cargo run --manifest-path cli/browser4-cli/Cargo.toml -- goto "https://en.wikipedia.org/wiki/Web_scraping"
 3. cargo run --manifest-path cli/browser4-cli/Cargo.toml -- tab-list
 4. cargo run --manifest-path cli/browser4-cli/Cargo.toml -- tab-new "https://news.ycombinator.com"
 5. cargo run --manifest-path cli/browser4-cli/Cargo.toml -- tab-new "https://en.wikipedia.org/wiki/Data_mining"
 6. cargo run --manifest-path cli/browser4-cli/Cargo.toml -- tab-list
 7. cargo run --manifest-path cli/browser4-cli/Cargo.toml -- tab-select 2
 8. cargo run --manifest-path cli/browser4-cli/Cargo.toml -- tab-select 1
 9. cargo run --manifest-path cli/browser4-cli/Cargo.toml -- tab-close 1
10. cargo run --manifest-path cli/browser4-cli/Cargo.toml -- tab-list
11. cargo run --manifest-path cli/browser4-cli/Cargo.toml -- tab-select 0
12. cargo run --manifest-path cli/browser4-cli/Cargo.toml -- tab-select 1
```

**Workarounds Applied During Task:**

None — all commands worked as documented (with caveats noted in issues below).

---

## Issues Found (9 issues)
> **Review complete:** 0 approved, 9 deferred/rejected

### Issue 1: tab-new reports misleading tab index

**Severity:** Medium
**Category:** UX / Product

#### Review Result

**Decision:** WONTFIX

**Summary:** - Query the actual tab list after creation and report the verified index

---

### Issue 2: Snapshot and Page info after tab-new show previous page content

**Severity:** Medium
**Category:** Reliability / UX

#### Review Result

**Decision:** WONTFIX

**Summary:** - Wait for the new tab's page to reach at least `DOMContentLoaded` before capturing the automatic snapshot

---

### Issue 3: Raw JSON leaks into human-readable output mode

**Severity:** Medium
**Category:** UX

#### Review Result

**Decision:** WONTFIX

**Summary:** - Route all MCP response data through the output formatter (JSON in `--json` mode, text in default mode)

---

### Issue 4: Duplicate "### Page" blocks in output

**Severity:** Low
**Category:** UX

#### Review Result

**Decision:** WONTFIX

**Summary:** - Identify and merge the two page-info printing paths

---

### Issue 5: tab-list triggers unnecessary full page snapshot

**Severity:** Low
**Category:** UX / Performance

#### Review Result

**Decision:** WONTFIX

**Summary:** - Add `tab-list` to `no_snapshot_commands()` so it doesn't trigger an automatic snapshot

---

### Issue 6: Tab management commands not documented in SKILL.md

**Severity:** Medium
**Category:** Discoverability

#### Review Result

**Decision:** WONTFIX

**Summary:** - Add a dedicated "Tabs" row to the Command Map table (separate from "Visual capture & viewport control")

---

### Issue 7: tab-close provides minimal, machine-oriented feedback

**Severity:** Low
**Category:** UX

#### Review Result

**Decision:** WONTFIX

**Summary:** - Format output as "Closed tab <index>: <title> (<url>)" using the tab info from the last `tab-list` or from the close response

---

### Issue 8: Command-specific help lacks examples and behavioral details

**Severity:** Low
**Category:** Discoverability

#### Review Result

**Decision:** WONTFIX

**Summary:** - Add `examples` and `notes` fields to `CommandDef` for commands that benefit from additional context

---

### Issue 9: Cargo build output noise obscures CLI output

**Severity:** Low
**Category:** UX

#### Review Result

**Decision:** WONTFIX

**Summary:** - Add a dedicated tip in `--help` output when the binary is run via `cargo run`: "Tip: Use `-q` to suppress build output"

---

## How to Reproduce

### Common Setup

1. Clone the repository and `cd` to the repo root.
2. Build the CLI: `cd cli/browser4-cli && cargo build`
3. The backend server starts automatically in dev mode.
4. All commands from repo root: `cd cli/browser4-cli && cargo run -- <command>`

### Per-Issue Reproduction Steps

#### Issue 1: tab-new reports misleading tab index

```
cargo run --manifest-path cli/browser4-cli/Cargo.toml -- goto "https://en.wikipedia.org/wiki/Web_scraping"
cargo run --manifest-path cli/browser4-cli/Cargo.toml -- tab-new "https://news.ycombinator.com"
# Reports: "Switched to tab 1"
cargo run --manifest-path cli/browser4-cli/Cargo.toml -- tab-new "https://en.wikipedia.org/wiki/Data_mining"
# Reports: "Switched to tab 2"
cargo run --manifest-path cli/browser4-cli/Cargo.toml -- tab-list
# Shows: index 0=Data mining, 1=HN, 2=Web scraping
```

#### Issue 2: Snapshot and Page info after tab-new show previous page content

```
cargo run --manifest-path cli/browser4-cli/Cargo.toml -- goto "https://en.wikipedia.org/wiki/Web_scraping"
cargo run --manifest-path cli/browser4-cli/Cargo.toml -- tab-new "https://news.ycombinator.com"
# Output:
# Switched to tab 1 (https://news.ycombinator.com)
# ### Page
# - Page URL: https://en.wikipedia.org/wiki/Web_scraping    <-- WRONG
# - Page Title: Web scraping - Wikipedia                   <-- WRONG
```

#### Issue 3: Raw JSON leaks into human-readable output mode

```
cargo run --manifest-path cli/browser4-cli/Cargo.toml -- tab-select 0
# Output includes: {"type":"ai.platon.browser4.chrome.PulsarWebDriver","description":"Driver#41"}
# Followed by normal human-readable Page/Snapshot output

cargo run --manifest-path cli/browser4-cli/Cargo.toml -- tab-new "https://news.ycombinator.com"
# Output includes: {"guid":"F539FDB4CADB1D3336A840FB4AB8725A","url":"https://news.ycombinator.com/"}
```

#### Issue 4: Duplicate "### Page" blocks in output

```
cargo run --manifest-path cli/browser4-cli/Cargo.toml -- tab-select 0
# ### Page
# - Page URL: https://en.wikipedia.org/wiki/Data_mining
# - Page Title: Data mining - Wikipedia
# ### Page                             <-- duplicate
# - Page URL: https://en.wikipedia.org/wiki/Data_mining
# - Page Title: Data mining - Wikipedia
```

#### Issue 5: tab-list triggers unnecessary full page snapshot

```
cargo run --manifest-path cli/browser4-cli/Cargo.toml -- tab-list
# Output includes:
# ### Snapshot
# [Snapshot](/home/vincent/.../snapshot-....yml)
# 💡 Tip: Run `snapshot -v 0` to see interactive element refs
```

#### Issue 6: Tab management commands not documented in SKILL.md

1. Read `skills/browser4-cli/SKILL.md` — the primary user-facing documentation for AI agents.
2. Search for tab management documentation.
3. Tab commands (`tab-list`, `tab-new`, `tab-close`, `tab-select`) are only mentioned in the Command Map (Section 3) under "Visual capture & viewport control" alongside `screenshot`, `scroll`, `wait`, `resize`.
4. No dedicated row, no usage examples, no explanation of tab lifecycle or index semantics.

#### Issue 7: tab-close provides minimal, machine-oriented feedback

```
cargo run --manifest-path cli/browser4-cli/Cargo.toml -- tab-close 1
# Output: true
# (Followed by the usual Page + Snapshot from the now-active tab)
```

#### Issue 8: Command-specific help lacks examples and behavioral details

```
cargo run --manifest-path cli/browser4-cli/Cargo.toml -- tab-new --help
# Output: browser4-cli tab-new [url]
#         Create a new tab
#         Arguments:
#           [url]  The URL to navigate to in the new tab

cargo run --manifest-path cli/browser4-cli/Cargo.toml -- tab-select --help
# Output: browser4-cli tab-select <index>
#         Select a browser tab
#         Arguments:
#           <index>  Zero-based tab index
```

#### Issue 9: Cargo build output noise obscures CLI output

Every command run via `cargo run` shows:
```
    Finished `dev` profile [unoptimized + debuginfo] target(s) in 0.48s
     Running `cli/browser4-cli/target/debug/browser4-cli <command>`
```
before the actual CLI output.
