# Issues: loop-monitoring

> **Source:** `20260728-155804-loop-monitoring.full.md` | **Date:** 20260728-155804 | **Mode:** dev

## Scenario Background

### Task

The task was largely completed. All 12 steps were attempted:

| Step | Description | Outcome |
|------|-------------|---------|
| 1 | Plain-text loop (health-check) | ❌ Failed — `command_run` timed out (no LLM key) |
| 2 | List active loops | ✅ Succeeded |
| 3 | Check health-check status | ✅ Succeeded (auto-cleaned after completion) |
| 4 | Shell mode loop (shell-test) | ✅ Succeeded — 2 iterations, 5s interval |
| 5 | Subcommand mode loop (cli-test) | ✅ Succeeded — 2 iterations, 5s interval |
| 6 | List loops again | ✅ Succeeded |
| 7 | Create paused loop, list, check status | ✅ Succeeded |
| 8 | Resume then pause | ✅ Succeeded |
| 9 | Stop paused loop | ✅ Succeeded |
| 10 | Timeout loop (30s) | ✅ Succeeded — 6 iterations, timeout message |
| 11 | Stop-all | ✅ Succeeded |
| 12 | Final list (confirm clean) | ✅ Succeeded — no persisted loops |

The only failure was Step 1, where the plain-text loop mode requires an LLM API key on the server for natural-language command interpretation. Shell mode and subcommand mode both worked flawlessly.

### Execution Context

**Key Commands:**

1. `./b4w.ps1 help` — discover available commands
2. `./b4w.ps1 loop --name health-check --count 2 --interval 10 "navigate to https://httpbin.org/get, take a snapshot, and report the page status"` — plain-text loop (failed with timeout)
3. `./b4w.ps1 loop --list` — verify loop state (found stale loop)
4. `./b4w.ps1 loop --status --name health-check` — check health-check (auto-cleaned)
5. `./b4w.ps1 loop --name shell-test --count 2 --interval 5 --shell "date ..."` — shell mode (worked)
6. `./b4w.ps1 loop --name cli-test --count 2 --interval 5 -- status` — subcommand mode (worked)
7. `./b4w.ps1 loop --list` — verify state
8. `./b4w.ps1 loop --name paused-test --count 3 --interval 5 --pause --shell "echo ..."` — create paused
9. `./b4w.ps1 loop --list` + `--status --name paused-test` — verify paused state
10. `./b4w.ps1 loop --resume --name paused-test` → `--pause --name paused-test` — resume/pause cycle
11. `./b4w.ps1 loop --stop --name paused-test` — stop
12. `./b4w.ps1 loop --name timeout-test --count 100 --interval 5 --timeout 30 --shell "echo ..."` — timeout test
13. `./b4w.ps1 loop --stop-all` — cleanup
14. `./b4w.ps1 loop --list` + `--history` — final verification

**Workarounds:** None needed for steps 2-12. Step 1 could be worked around by using `--` subcommand mode with explicit `goto`, `snapshot`, and `status` commands.

**Key decisions:** Read SKILL.md and loop.md reference before starting. Used `b4w.ps1` (PowerShell wrapper) from repo root as instructed.

```json
{
  "issues": [
    {
      "title": "Plain-text loop mode requires undocumented LLM API key",
      "severity": "Medium",
      "category": "Documentation",
      "reproduction": "./b4w.ps1 loop --name health-check --count 2 --interval 10 \"navigate to https://httpbin.org/get, take a snapshot, and report the page status\"",
      "expected": "Either the loop executes the natural-language task successfully, or a clear error message explains that an LLM API key is required for plain-text mode.",
      "actual": "Both iterations failed with: [ERROR] Iteration 1: HTTP request timed out [tool=command_run, endpoint=http://localhost:8182/mcp/call-tool, timeout=30s]",
      "rootCause": "The plain-text loop mode submits the task to the server's `command_run` MCP tool, which attempts to interpret natural language via an LLM. Without a configured LLM API key, the server request hangs until the 30s HTTP timeout. The error message is misleading — it implies a network/connectivity issue rather than a missing configuration prerequisite.",
      "codePointer": "",
      "suggestion": "- Add a prerequisite check in the loop command that detects when plain-text mode is used and warns if no LLM key is configured\n- The server should return a fast-fail error (not a timeout) when command_run is invoked without an LLM key\n- Add a note in both `loop --help` and the loop.md reference doc that plain-text mode requires an LLM API key on the server\n- Consider adding a `--require-llm` or similar flag, or auto-detect and suggest subcommand mode as an alternative"
    },
    {
      "title": "Stale loop state persists from previous sessions with no staleness indicator",
      "severity": "Low",
      "category": "UX",
      "reproduction": "Run ./b4w.ps1 loop --list after a previous session left a named loop running.",
      "expected": "The list should clearly indicate that a loop is stale (from a prior session, not actively running), or auto-detect and mark orphaned loops.",
      "actual": "A loop named \"(default)\" from a prior session showed as \"▶ running\" with \"2 iters\" completed, even though no process was actively executing it. There was no way to distinguish between genuinely running loops and orphaned state files.",
      "rootCause": "Loop state is persisted to disk and the 'running' status is written before a background process is spawned. If the background process dies (e.g., session ends) without updating the state to 'paused' or 'completed', the state file retains 'running' indefinitely. The --list command reads the status field directly from the JSON file without checking whether a corresponding OS process exists.",
      "codePointer": "",
      "suggestion": "- When listing loops, check whether a corresponding OS process (PID) exists and show 'orphaned' or 'stale' instead of 'running' when no process is found\n- Store the background PID in the state file so staleness can be detected\n- Add a timestamp-based staleness heuristic: if updatedAt is more than N minutes ago and status is 'running', show a warning\n- Add a --prune or --cleanup-stale flag to remove orphaned loop state files"
    },
    {
      "title": "Shell variable expansion occurs before reaching the loop shell due to b4w.ps1 quoting",
      "severity": "Medium",
      "category": "Product",
      "reproduction": "./b4w.ps1 loop --name timeout-test --count 100 --interval 5 --timeout 30 --shell \"echo 'iteration at \\$(date +%H:%M:%S)'\"",
      "expected": "Each iteration prints a different timestamp as $(date) is evaluated by the loop's shell at iteration time.",
      "actual": "All 6 iterations printed the same timestamp (the time of command invocation): the \\$(date ...) was expanded by PowerShell's argument parser before reaching the Rust binary.",
      "rootCause": "The b4w.ps1 PowerShell wrapper processes arguments through PowerShell's argument binder before passing them to the Rust binary. Even with backslash-escaping (\$), PowerShell may expand the subshell expression. The SKILL.md warns about shell quoting issues on Windows and recommends using files for complex content, but this particular failure mode (subshell expansion in --shell mode) isn't documented.",
      "codePointer": "b4w.ps1 (argument forwarding logic)",
      "suggestion": "- Document this specific pitfall in the loop.md reference: subshell expressions like $(...) in --shell mode may be expanded by the wrapper before reaching the loop\n- Suggest using single quotes around the entire --shell argument when using b4w.sh instead of b4w.ps1, or writing the shell command to a file and using --file\n- Consider adding a --shell-file flag to read the shell command from a file, avoiding quoting issues entirely"
    },
    {
      "title": "Main help output gives minimal loop documentation; detailed help requires separate --help invocation",
      "severity": "Low",
      "category": "Discoverability",
      "reproduction": "Run ./b4w.ps1 help and observe the loop entry.",
      "expected": "The main help includes enough detail or examples for a first-time user to understand the three loop modes and basic syntax without needing a separate --help invocation.",
      "actual": "The main help shows a single dense line: 'loop [task] Execute a task repeatedly on an interval...' with no examples. The user must run `loop --help` to see the full reference with examples and mode descriptions. The SKILL.md reference doc at skills/browser4-cli/references/loop.md is comprehensive but requires knowing it exists.",
      "rootCause": "The main help output is designed as a command index, with detailed help in per-command --help output and reference docs. This is a reasonable architecture but adds a discoverability step for complex commands like loop.",
      "codePointer": "",
      "suggestion": "- Add 1-2 brief usage examples to the loop entry in the main help output\n- Add a pointer to `loop --help` in the main help entry: 'See `browser4-cli loop --help` for examples and all flags'\n- Consider adding 'loop' as a category in the `--help` filter list (currently: nav, extract, session, kb, agent, swarm, crawl)"
    },
    {
      "title": "Failed iterations in plain-text mode still count toward completion, state auto-cleaned",
      "severity": "Low",
      "category": "Reliability",
      "reproduction": "Run a plain-text loop that fails all iterations (e.g., health-check with no LLM key).",
      "expected": "The loop should either halt on repeated failures or preserve the state file so the user can inspect what went wrong. Completed-with-errors should be distinguished from completed-successfully.",
      "actual": "The loop reported 'Loop finished — 2 iteration(s) completed' and auto-cleaned the state file, even though all iterations failed with errors. The history preserved a 'count reached' entry with no indication that all iterations failed.",
      "rootCause": "The loop's completion logic treats reaching the count limit as successful completion regardless of individual iteration outcomes. The auto-clean behavior is triggered by any normal completion (count reached or timeout), with no check on whether iterations succeeded.",
      "codePointer": "",
      "suggestion": "- Track failed iteration count separately and show it in the completion summary\n- Consider adding a --stop-on-error flag that halts the loop after N consecutive failures\n- Don't auto-clean state when all iterations failed — preserve it for debugging\n- Record iteration success/failure in the history entry so users can see at a glance whether the loop was healthy"
    },
    {
      "title": "No JSON output mode tested for loop command control operations",
      "severity": "Low",
      "category": "UX",
      "reproduction": "Run ./b4w.ps1 --json loop --list",
      "expected": "Structured JSON output for --list, --status, --history, and other control operations.",
      "actual": "Not tested in this session. The loop.md reference documents JSON output for execution mode but doesn't explicitly document JSON output format for control operations like --list and --status.",
      "rootCause": "Documentation gap — the loop.md reference shows JSON output format only for execution mode (iterations array), not for the control operations that users are likely to script around (--list, --status).",
      "codePointer": "",
      "suggestion": "- Document JSON output format for --list, --status, and --history in loop.md\n- Ensure all control operations produce consistent JSON envelopes when --json is used"
    },
    {
      "title": "Loop history grows unboundedly with no pruning mechanism other than the 200-entry cap",
      "severity": "Low",
      "category": "UX",
      "reproduction": "Run ./b4w.ps1 loop --history after multiple test sessions.",
      "expected": "A reasonable retention policy or a way to filter/clear history.",
      "actual": "37 entries accumulated from multiple test sessions going back weeks. The 200-entry cap prevents unbounded growth but there's no --clear-history flag or age-based pruning.",
      "rootCause": "The history feature is append-only to a JSONL file with a 200-entry cap. There is no mechanism to clear history, filter by age, or prune entries.",
      "codePointer": "",
      "suggestion": "- Add a --clear-history flag to reset the history log\n- Consider age-based pruning (e.g., entries older than 30 days)\n- Add --name and --age filters to --history for targeted queries"
    }
  ],
  "assessment": {
    "completionStatus": "Partially Successful — 11 of 12 steps succeeded. The only failure was the plain-text loop mode (Step 1), which requires an LLM API key not configured on this server. The same task could be accomplished via subcommand mode (-- goto + snapshot + status). Shell mode, subcommand mode, pause/resume/stop lifecycle, timeout, bulk operations, and cleanup all worked correctly.",
    "successRate": "92% — 11/12 steps completed successfully",
    "issuesFound": 7,
    "majorBlockers": "Plain-text loop mode silently fails with a misleading timeout error when no LLM API key is configured. This blocks the most user-friendly entry point to the loop feature. The workaround (using -- subcommand mode) works but requires users to know the individual browser4-cli commands.",
    "mostConfusingAspects": "1) The distinction between plain-text, --shell, and -- modes is documented but the plain-text mode's LLM dependency is not surfaced upfront. 2) The stale default loop appearing in --list was surprising — as a first-time user, I would wonder 'why is there already a loop running?' 3) The shell variable expansion issue with b4w.ps1 means the --shell mode doesn't behave as a naive user would expect.",
    "mostValuableImprovements": "1) Fast-fail error when plain-text mode is used without an LLM key, with a clear message suggesting alternatives. 2) Stale loop detection in --list by checking OS process existence. 3) Adding brief examples to the main help entry for loop. 4) A --stop-on-error or --max-failures flag to prevent loops from silently completing with all-failed iterations.",
    "usabilityRating": 7
  }
}
```

---

## Issues Found (0)

No issues could be parsed from Section C of the agent output.

See `20260728-155804-loop-monitoring.full.md` for the complete evaluation output.

